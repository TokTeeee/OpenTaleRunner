# v0.6.3 Item Affix Pool Expansion + Staff/Holy Symbol + Equipment Effects in Combat

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 扩展 affix 池抗性/元素词条 + 新增法杖/圣印记施法武器 + 装备抗性/MP 效果接入战斗计算

**Architecture:** 数据层 (affixPools 扩展 + Affix 接口加 minSubCategory) → 战斗层 (getEquipmentResistances/getEquipmentMPBonus + 初始化合并) → 测试 + 回归

**Tech Stack:** TypeScript (strict mode) + Zustand + React + Vitest + ESLint

---

## File Structure

### 修改文件

```
client/src/
├── data/affixPools.ts          # 扩展抗性/元素词条 + 法杖/圣印记词条
├── data/affixPool.ts           # drawBuffs/rollDebuff 加 minSubCategory 过滤
├── types/item.ts               # EffectType 加 'mp_bonus'
├── services/combat/ActionResolver.ts  # 新增 getEquipmentResistances / getEquipmentMPBonus
├── services/combat/debugCombatStarter.ts  # 合并装备抗性/MP
├── services/combat/combatTools.ts      # 合并装备抗性/MP
├── data/debugPresets.ts        # debug_ability 预设加法杖
client/tests/
├── data/affixPools.test.ts     # 扩展词条测试
├── data/affixPool.test.ts      # 子类过滤测试
├── services/combat/ActionResolver.test.ts  # 装备抗性测试
├── services/combat/regression.test.ts      # 回归测试
```

---

## 约定

- **测试命令**: `cd client && npx vitest run tests/path/to/test.test.ts`
- **类型检查**: `cd client && npx tsc --noEmit`
- **Lint**: `cd client && npx eslint src/ tests/`
- **commit 格式**: conventional commits

---

## Phase 1 — 数据层扩展

### Task 1: types/item.ts 加 'mp_bonus' EffectType

**Files:**
- Modify: `client/src/types/item.ts`

- [ ] **Step 1: 在 EffectType 联合类型中追加 'mp_bonus'**

找到 `EffectType` 联合类型定义，在 `'elemental_resist'` 后追加:

```typescript
| 'elemental_resist' | 'mp_bonus'
```

- [ ] **Step 2: typecheck**

Run: `cd client && npx tsc --noEmit`
Expected: 0 errors (mp_bonus 是新类型，不影响现有代码)

- [ ] **Step 3: commit**

```bash
git add client/src/types/item.ts
git commit -m "feat(v0.6.3): EffectType 加 'mp_bonus'"
```

---

### Task 2: affixPools.ts 扩展抗性/元素词条

**Files:**
- Modify: `client/src/data/affixPools.ts`
- Test: `client/tests/data/affixPools.test.ts`

- [ ] **Step 1: 写抗性词条测试**

在 `client/tests/data/affixPools.test.ts` 中追加:

```typescript
describe('v0.6.3 抗性词条', () => {
  it('防具池有 8 元素单抗词条 (精良)', () => {
    const armorBuffs = AFFIX_POOLS.armor.buffs;
    const elements = ['fire', 'ice', 'lightning', 'wind', 'earth', 'arcane', 'holy', 'shadow'];
    for (const el of elements) {
      const found = armorBuffs.find(a => a.effect.id === `arm_resist_${el}_1`);
      expect(found, `missing arm_resist_${el}_1`).toBeDefined();
      expect(found!.effect.type).toBe('elemental_resist');
    }
  });

  it('防具池有稀有档位抗性词条 (fire/ice/lightning)', () => {
    const armorBuffs = AFFIX_POOLS.armor.buffs;
    for (const el of ['fire', 'ice', 'lightning']) {
      const found = armorBuffs.find(a => a.effect.id === `arm_resist_${el}_2`);
      expect(found, `missing arm_resist_${el}_2`).toBeDefined();
    }
  });

  it('防具池有全抗词条 (史诗)', () => {
    const found = AFFIX_POOLS.armor.buffs.find(a => a.effect.id === 'arm_resist_all_1');
    expect(found).toBeDefined();
    expect(found!.minQuality).toBe('史诗');
  });

  it('饰品池有抗性词条', () => {
    const accBuffs = AFFIX_POOLS.accessory.buffs;
    const found = accBuffs.find(a => a.effect.id === 'acc_resist_fire_1');
    expect(found).toBeDefined();
  });

  it('武器池有 8 元素伤害词条', () => {
    const weaponBuffs = AFFIX_POOLS.weapon.buffs;
    const elements = ['fire', 'ice', 'lightning', 'wind', 'earth', 'arcane', 'holy', 'shadow'];
    for (const el of elements) {
      const found = weaponBuffs.find(a => a.effect.id === `wpn_elem_${el}_1`);
      expect(found, `missing wpn_elem_${el}_1`).toBeDefined();
      expect(found!.effect.type).toBe('elemental_damage');
    }
  });

  it('旧词条 arm_resist_1 已被替换', () => {
    const found = AFFIX_POOLS.armor.buffs.find(a => a.effect.id === 'arm_resist_1');
    expect(found).toBeUndefined();
  });

  it('旧词条 wpn_elem_1 已被替换', () => {
    const found = AFFIX_POOLS.weapon.buffs.find(a => a.effect.id === 'wpn_elem_1');
    expect(found).toBeUndefined();
  });
});
```

- [ ] **Step 2: 运行测试，确认 FAIL**

Run: `cd client && npx vitest run tests/data/affixPools.test.ts`
Expected: FAIL (新词条尚未添加)

- [ ] **Step 3: 替换 affixPools.ts 中的 ARMOR_BUFFS**

将 `ARMOR_BUFFS` 替换为:

```typescript
const ARMOR_BUFFS: readonly Affix[] = [
  { weight: 8, minQuality: '普通', effect: { id: 'arm_def_1', type: 'defense_bonus', value: 3, description: '+3 防御' } },
  { weight: 5, minQuality: '精良', effect: { id: 'arm_hp_1', type: 'hp_max_bonus', value: 10, description: '最大 HP +10' } },
  // v0.6.3: 8 元素单抗 (精良)
  { weight: 4, minQuality: '精良', effect: { id: 'arm_resist_fire_1', type: 'elemental_resist', value: { fire: 20 }, description: '火抗 +20%' } },
  { weight: 4, minQuality: '精良', effect: { id: 'arm_resist_ice_1', type: 'elemental_resist', value: { ice: 20 }, description: '冰抗 +20%' } },
  { weight: 4, minQuality: '精良', effect: { id: 'arm_resist_lightning_1', type: 'elemental_resist', value: { lightning: 20 }, description: '雷抗 +20%' } },
  { weight: 3, minQuality: '精良', effect: { id: 'arm_resist_wind_1', type: 'elemental_resist', value: { wind: 20 }, description: '风抗 +20%' } },
  { weight: 3, minQuality: '精良', effect: { id: 'arm_resist_earth_1', type: 'elemental_resist', value: { earth: 20 }, description: '土抗 +20%' } },
  { weight: 2, minQuality: '精良', effect: { id: 'arm_resist_arcane_1', type: 'elemental_resist', value: { arcane: 20 }, description: '奥抗 +20%' } },
  { weight: 2, minQuality: '精良', effect: { id: 'arm_resist_holy_1', type: 'elemental_resist', value: { holy: 20 }, description: '圣抗 +20%' } },
  { weight: 2, minQuality: '精良', effect: { id: 'arm_resist_shadow_1', type: 'elemental_resist', value: { shadow: 20 }, description: '暗抗 +20%' } },
  // v0.6.3: 稀有档位
  { weight: 2, minQuality: '稀有', effect: { id: 'arm_resist_fire_2', type: 'elemental_resist', value: { fire: 40 }, description: '火抗 +40%' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'arm_resist_ice_2', type: 'elemental_resist', value: { ice: 40 }, description: '冰抗 +40%' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'arm_resist_lightning_2', type: 'elemental_resist', value: { lightning: 40 }, description: '雷抗 +40%' } },
  // v0.6.3: 全抗 (史诗)
  { weight: 1, minQuality: '史诗', effect: { id: 'arm_resist_all_1', type: 'elemental_resist', value: { fire: 10, ice: 10, lightning: 10, wind: 10, earth: 10, arcane: 10, holy: 10, shadow: 10 }, description: '全抗 +10%' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'arm_reflect_1', type: 'damage_reflect', value: 10, description: '反伤 10%' } },
];
```

- [ ] **Step 4: 替换 WEAPON_BUFFS 中的元素词条**

将 `WEAPON_BUFFS` 中的 `wpn_elem_1` 替换为 8 元素词条:

```typescript
const WEAPON_BUFFS: readonly Affix[] = [
  { weight: 8, minQuality: '普通', effect: { id: 'wpn_dmg_1', type: 'damage_bonus', value: 5, description: '+5 攻击' } },
  { weight: 4, minQuality: '精良', effect: { id: 'wpn_crit_1', type: 'critical', value: 10, description: '暴击率 +10%' } },
  // v0.6.3: 8 元素伤害词条 (替代单一 wpn_elem_1)
  { weight: 4, minQuality: '精良', effect: { id: 'wpn_elem_fire_1', type: 'elemental_damage', value: { fire: 3 }, description: '附加 3 点火属性' } },
  { weight: 4, minQuality: '精良', effect: { id: 'wpn_elem_ice_1', type: 'elemental_damage', value: { ice: 3 }, description: '附加 3 点冰属性' } },
  { weight: 3, minQuality: '精良', effect: { id: 'wpn_elem_lightning_1', type: 'elemental_damage', value: { lightning: 3 }, description: '附加 3 点雷属性' } },
  { weight: 3, minQuality: '精良', effect: { id: 'wpn_elem_wind_1', type: 'elemental_damage', value: { wind: 3 }, description: '附加 3 点风属性' } },
  { weight: 2, minQuality: '精良', effect: { id: 'wpn_elem_earth_1', type: 'elemental_damage', value: { earth: 3 }, description: '附加 3 点土属性' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'wpn_elem_arcane_1', type: 'elemental_damage', value: { arcane: 3 }, description: '附加 3 点奥术属性' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'wpn_elem_holy_1', type: 'elemental_damage', value: { holy: 3 }, description: '附加 3 点神圣属性' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'wpn_elem_shadow_1', type: 'elemental_damage', value: { shadow: 3 }, description: '附加 3 点暗影属性' } },
  { weight: 3, minQuality: '稀有', effect: { id: 'wpn_spd_1', type: 'attack_speed', value: 1, description: '攻速 +1' } },
  { weight: 2, minQuality: '史诗', effect: { id: 'wpn_steal_1', type: 'hp_steal', value: 2, description: '击杀回 2 HP' } },
];
```

- [ ] **Step 5: 在 ACCESSORY_BUFFS 中追加抗性词条**

在 `ACCESSORY_BUFFS` 数组末尾 (在 `acc_light_1` 之后) 追加:

```typescript
  // v0.6.3: 饰品抗性词条
  { weight: 3, minQuality: '精良', effect: { id: 'acc_resist_fire_1', type: 'elemental_resist', value: { fire: 15 }, description: '火抗 +15%' } },
  { weight: 3, minQuality: '精良', effect: { id: 'acc_resist_ice_1', type: 'elemental_resist', value: { ice: 15 }, description: '冰抗 +15%' } },
  { weight: 3, minQuality: '精良', effect: { id: 'acc_resist_lightning_1', type: 'elemental_resist', value: { lightning: 15 }, description: '雷抗 +15%' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'acc_resist_holy_1', type: 'elemental_resist', value: { holy: 15 }, description: '圣抗 +15%' } },
  { weight: 2, minQuality: '稀有', effect: { id: 'acc_resist_shadow_1', type: 'elemental_resist', value: { shadow: 15 }, description: '暗抗 +15%' } },
  { weight: 1, minQuality: '传说', effect: { id: 'acc_resist_all_1', type: 'elemental_resist', value: { fire: 10, ice: 10, lightning: 10, wind: 10, earth: 10, arcane: 10, holy: 10, shadow: 10 }, description: '全抗 +10%' } },
```

- [ ] **Step 6: 运行测试，确认 PASS**

Run: `cd client && npx vitest run tests/data/affixPools.test.ts`
Expected: PASS

- [ ] **Step 7: commit**

```bash
git add client/src/data/affixPools.ts client/tests/data/affixPools.test.ts
git commit -m "feat(v0.6.3): affixPools 扩展 8 元素抗性/伤害词条"
```

---

### Task 3: Affix 接口加 minSubCategory + 法杖/圣印记词条

**Files:**
- Modify: `client/src/data/affixPools.ts` (Affix 接口 + 法杖/圣印记词条)
- Modify: `client/src/data/affixPool.ts` (drawBuffs/rollDebuff 过滤)
- Test: `client/tests/data/affixPool.test.ts`

- [ ] **Step 1: 扩展 Affix 接口**

在 `affixPools.ts` 的 `Affix` 接口中追加:

```typescript
export interface Affix {
  readonly weight: number;
  readonly minQuality: ItemQuality;
  readonly effect: ItemEffect;
  /** 可选: 仅当物品 subCategory 匹配时才纳入候选 */
  readonly minSubCategory?: string;
}
```

- [ ] **Step 2: 在 WEAPON_BUFFS 末尾追加法杖/圣印记词条**

```typescript
  // v0.6.3: 法杖专属词条
  { weight: 5, minQuality: '精良', minSubCategory: 'staff', effect: { id: 'staff_int_1', type: 'attribute_mod', value: { INT: 2 }, description: 'INT +2' } },
  { weight: 4, minQuality: '精良', minSubCategory: 'staff', effect: { id: 'staff_mp_1', type: 'mp_bonus', value: 10, description: 'MP +10' } },
  { weight: 3, minQuality: '稀有', minSubCategory: 'staff', effect: { id: 'staff_elem_fire', type: 'elemental_damage', value: { fire: 5 }, description: '附加 5 点火属性' } },
  { weight: 3, minQuality: '稀有', minSubCategory: 'staff', effect: { id: 'staff_elem_ice', type: 'elemental_damage', value: { ice: 5 }, description: '附加 5 点冰属性' } },
  { weight: 2, minQuality: '稀有', minSubCategory: 'staff', effect: { id: 'staff_elem_arcane', type: 'elemental_damage', value: { arcane: 5 }, description: '附加 5 点奥术属性' } },
  // v0.6.3: 圣印记专属词条
  { weight: 5, minQuality: '精良', minSubCategory: 'holy_symbol', effect: { id: 'symbol_wis_1', type: 'attribute_mod', value: { WIS: 2 }, description: 'WIS +2' } },
  { weight: 4, minQuality: '精良', minSubCategory: 'holy_symbol', effect: { id: 'symbol_mp_1', type: 'mp_bonus', value: 10, description: 'MP +10' } },
  { weight: 3, minQuality: '稀有', minSubCategory: 'holy_symbol', effect: { id: 'symbol_elem_holy', type: 'elemental_damage', value: { holy: 5 }, description: '附加 5 点神圣属性' } },
  { weight: 2, minQuality: '稀有', minSubCategory: 'holy_symbol', effect: { id: 'symbol_elem_shadow', type: 'elemental_damage', value: { shadow: 5 }, description: '附加 5 点暗影属性' } },
```

- [ ] **Step 3: 修改 drawAffixes 签名，接收 subCategory 参数**

在 `affixPool.ts` 中修改 `drawAffixes` 函数签名:

```typescript
export function drawAffixes(
  category: ItemCategory,
  quality: ItemQuality,
  rng: RNG = defaultRng,
  subCategory?: string,
): ItemEffect[] {
  if (!isPoolKey(category)) return [];
  const range = QUALITY_AFFIX_RANGE[quality];
  const buffCount = range.min === range.max
    ? range.min
    : range.min + rng.int(range.max - range.min + 1);
  const buffs = drawBuffs(category, quality, buffCount, rng, subCategory);
  const debuff = rollDebuff(category, quality, rng, subCategory);
  return debuff ? [...buffs, debuff] : buffs;
}
```

- [ ] **Step 4: 修改 drawBuffs 和 rollDebuff，增加 subCategory 过滤**

```typescript
export function drawBuffs(
  category: AffixPoolKey,
  quality: ItemQuality,
  count: number,
  rng: RNG = defaultRng,
  subCategory?: string,
): ItemEffect[] {
  if (count <= 0) return [];
  const candidates = AFFIX_POOLS[category].buffs.filter((a) =>
    qualityAtLeast(quality, a.minQuality) &&
    (!a.minSubCategory || a.minSubCategory === subCategory)
  );
  if (candidates.length === 0) return [];
  return Array.from({ length: count }, () => weightedPick(candidates, rng).effect);
}

export function rollDebuff(
  category: AffixPoolKey,
  quality: ItemQuality,
  rng: RNG = defaultRng,
  _subCategory?: string,
): ItemEffect | null {
  if (rng.next() >= DEBUFF_PROBABILITY[quality]) return null;
  const candidates = AFFIX_POOLS[category].debuffs.filter((a) => qualityAtLeast(quality, a.minQuality));
  if (candidates.length === 0) return null;
  return weightedPick(candidates, rng).effect;
}
```

- [ ] **Step 5: 写子类过滤测试**

在 `client/tests/data/affixPool.test.ts` 中追加:

```typescript
describe('v0.6.3 minSubCategory 过滤', () => {
  it('法杖专属词条在 subCategory=staff 时可抽取', () => {
    const rng = { next: () => 0.99, int: (n: number) => n - 1 }; // 选最后一个
    const buffs = drawBuffs('weapon', '精良', 20, rng, 'staff');
    const hasStaffAffix = buffs.some(b => b.id === 'staff_int_1' || b.id === 'staff_mp_1');
    expect(hasStaffAffix).toBe(true);
  });

  it('法杖专属词条在 subCategory 未指定时不可抽取', () => {
    const rng = { next: () => 0.99, int: (n: number) => n - 1 };
    const buffs = drawBuffs('weapon', '精良', 20, rng);
    const hasStaffAffix = buffs.some(b => b.id === 'staff_int_1' || b.id === 'staff_mp_1');
    expect(hasStaffAffix).toBe(false);
  });

  it('圣印记专属词条在 subCategory=holy_symbol 时可抽取', () => {
    const rng = { next: () => 0.99, int: (n: number) => n - 1 };
    const buffs = drawBuffs('weapon', '精良', 20, rng, 'holy_symbol');
    const hasSymbolAffix = buffs.some(b => b.id === 'symbol_wis_1' || b.id === 'symbol_mp_1');
    expect(hasSymbolAffix).toBe(true);
  });
});
```

- [ ] **Step 6: 运行测试，确认 PASS**

Run: `cd client && npx vitest run tests/data/affixPool.test.ts`
Expected: PASS

- [ ] **Step 7: typecheck + commit**

Run: `cd client && npx tsc --noEmit`
Expected: 0 errors

```bash
git add client/src/data/affixPools.ts client/src/data/affixPool.ts client/tests/data/affixPool.test.ts
git commit -m "feat(v0.6.3): Affix 接口加 minSubCategory + 法杖/圣印记词条 + 子类过滤"
```

---

## Phase 2 — 装备效果接入战斗

### Task 4: ActionResolver 新增 getEquipmentResistances / getEquipmentMPBonus

**Files:**
- Modify: `client/src/services/combat/ActionResolver.ts`
- Test: `client/tests/services/combat/ActionResolver.test.ts`

- [ ] **Step 1: 写装备抗性汇总测试**

在 `ActionResolver.test.ts` 中追加:

```typescript
describe('v0.6.3 装备效果汇总', () => {
  it('getEquipmentResistances 汇总 3 槽位抗性', () => {
    const equipped = {
      weapon: null,
      armor: {
        effects: [
          { id: 'r1', type: 'elemental_resist', value: { fire: 20 }, description: '火抗' },
        ],
      } as any,
      accessory: {
        effects: [
          { id: 'r2', type: 'elemental_resist', value: { ice: 15, fire: 10 }, description: '双抗' },
        ],
      } as any,
    };
    const result = getEquipmentResistances(equipped);
    expect(result.fire).toBe(30);  // 20 + 10
    expect(result.ice).toBe(15);
  });

  it('getEquipmentResistances 无装备返回空对象', () => {
    const equipped = { weapon: null, armor: null, accessory: null };
    const result = getEquipmentResistances(equipped);
    expect(Object.keys(result)).toHaveLength(0);
  });

  it('getEquipmentMPBonus 汇总 MP 加成', () => {
    const equipped = {
      weapon: {
        effects: [
          { id: 'm1', type: 'mp_bonus', value: 5, description: 'MP+5' },
        ],
      } as any,
      armor: null,
      accessory: {
        effects: [
          { id: 'm2', type: 'mp_bonus', value: 10, description: 'MP+10' },
        ],
      } as any,
    };
    expect(getEquipmentMPBonus(equipped)).toBe(15);
  });

  it('getEquipmentMPBonus 无装备返回 0', () => {
    const equipped = { weapon: null, armor: null, accessory: null };
    expect(getEquipmentMPBonus(equipped)).toBe(0);
  });
});
```

- [ ] **Step 2: 运行测试，确认 FAIL**

Run: `cd client && npx vitest run tests/services/combat/ActionResolver.test.ts -t "装备效果汇总"`
Expected: FAIL (函数不存在)

- [ ] **Step 3: 在 ActionResolver.ts 中实现两个函数**

在 `getEquipmentAttributeMods` 函数之后追加:

```typescript
/** 从装备效果汇总元素抗性 (elemental_resist 词条). */
export function getEquipmentResistances(
  equipped: Combatant['equipped'],
): Partial<ElementalResistances> {
  const result: Partial<ElementalResistances> = {};
  const slots = [equipped.weapon, equipped.armor, equipped.accessory];
  for (const item of slots) {
    if (!item?.effects) continue;
    for (const eff of item.effects) {
      if (eff.type === 'elemental_resist' && typeof eff.value === 'object' && eff.value !== null) {
        const v = eff.value as Record<string, unknown>;
        for (const [element, val] of Object.entries(v)) {
          if (typeof val === 'number') {
            result[element as keyof ElementalResistances] =
              (result[element as keyof ElementalResistances] ?? 0) + val;
          }
        }
      }
    }
  }
  return result;
}

/** 从装备效果汇总 MP 加成 (mp_bonus 词条). */
export function getEquipmentMPBonus(equipped: Combatant['equipped']): number {
  let bonus = 0;
  const slots = [equipped.weapon, equipped.armor, equipped.accessory];
  for (const item of slots) {
    if (!item?.effects) continue;
    for (const eff of item.effects) {
      if (eff.type === 'mp_bonus' && typeof eff.value === 'number') {
        bonus += eff.value;
      }
    }
  }
  return bonus;
}
```

同时在文件顶部 import 中追加 `ElementalResistances`:

```typescript
import type { ElementalResistances } from '../../types/character';
```

- [ ] **Step 4: 运行测试，确认 PASS**

Run: `cd client && npx vitest run tests/services/combat/ActionResolver.test.ts -t "装备效果汇总"`
Expected: PASS

- [ ] **Step 5: commit**

```bash
git add client/src/services/combat/ActionResolver.ts client/tests/services/combat/ActionResolver.test.ts
git commit -m "feat(v0.6.3): getEquipmentResistances / getEquipmentMPBonus 装备效果汇总"
```

---

### Task 5: 战斗初始化合并装备抗性/MP

**Files:**
- Modify: `client/src/services/combat/debugCombatStarter.ts`
- Modify: `client/src/services/combat/combatTools.ts`

- [ ] **Step 1: 在 debugCombatStarter.ts 中合并装备抗性**

在 `makeDebugMageCharacter` 函数中，找到 combatant 构造处，在 `elementalResistances` 字段中使用 `getEquipmentResistances`:

```typescript
import { getEquipmentResistances, getEquipmentMPBonus } from './ActionResolver';

// 在 makeDebugMageCharacter 中:
const equipResists = getEquipmentResistances(character.equipped);
const mpBonus = getEquipmentMPBonus(character.equipped);

// combatant 构造:
elementalResistances: {
  ...character.elementalResistances,
  ...Object.fromEntries(
    Object.entries(equipResists).map(([k, v]) => [k, (character.elementalResistances[k] ?? 0) + v])
  ),
} as ElementalResistances,
maxMp: character.maxMp + mpBonus,
mp: character.mp + mpBonus,
```

- [ ] **Step 2: 在 combatTools.ts 的 startCombat handler 中同样合并**

在 `startCombatHandler` 中找到 player combatant 构造处，同样应用 `getEquipmentResistances` 和 `getEquipmentMPBonus`。

- [ ] **Step 3: typecheck**

Run: `cd client && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 4: commit**

```bash
git add client/src/services/combat/debugCombatStarter.ts client/src/services/combat/combatTools.ts
git commit -m "feat(v0.6.3): 战斗初始化合并装备抗性/MP 到 combatant"
```

---

### Task 6: debugPresets 加法杖 + 装备抗性战斗回归测试

**Files:**
- Modify: `client/src/data/debugPresets.ts`
- Modify: `client/tests/services/combat/regression.test.ts`

- [ ] **Step 1: 在 debug_ability 预设中添加法杖**

在 `debugPresets.ts` 的 `debug_ability` 预设中，将 `equipped.weapon` 从 `null` 改为法杖:

```typescript
equipped: {
  weapon: {
    name: '学徒法杖',
    category: 'weapon' as const,
    subCategory: 'staff',
    quality: '精良' as const,
    effects: [
      { id: 'base_int', type: 'attribute_mod' as const, value: { INT: 1 }, description: 'INT +1' },
      { id: 'base_mp', type: 'mp_bonus' as const, value: 5, description: 'MP +5' },
    ],
  },
  armor: null,
  accessory: null,
},
```

- [ ] **Step 2: 写装备抗性战斗回归测试**

在 `regression.test.ts` 中追加:

```typescript
it('v0.6.3 装备抗性在战斗中生效', () => {
  // 构造一个有火抗装备的 combatant
  useCombatStore.setState({
    combatants: {
      p1: makePlayer({
        elementalResistances: { ...ZERO_RESISTANCES, fire: 20 }, // 装备火抗+20
      }),
      e1: makeEnemy('e1'),
    },
  });
  const resolver = createActionResolver({ roll: makeConstRoll([20, 4]), qte: () => ({ accuracy: 1, modifier: 0, type: 'magic' as const }) });
  const hpBefore = useCombatStore.getState().combatants.e1.hp;
  // 释放火球术 (fire element)
  resolver.resolve({ kind: 'ability', userId: 'p1', abilityId: 'spell_fire_bolt', targetId: 'e1' } as any, useCombatStore.getState());
  // 火抗+20 → 10% 减伤, 伤害应比无抗性时少
  const e1After = useCombatStore.getState().combatants.e1;
  expect(e1After.hp).toBeGreaterThan(hpBefore - 7); // 无抗性时约 7 伤害
});
```

- [ ] **Step 3: 运行回归测试**

Run: `cd client && npx vitest run tests/services/combat/regression.test.ts`
Expected: PASS

- [ ] **Step 4: commit**

```bash
git add client/src/data/debugPresets.ts client/tests/services/combat/regression.test.ts
git commit -m "feat(v0.6.3): debug 预设加法杖 + 装备抗性战斗回归测试"
```

---

## Phase 3 — 全量验证

### Task 7: 全量验证

**Files:** (无)

- [ ] **Step 1: typecheck**

Run: `cd client && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 2: lint**

Run: `cd client && npx eslint src/ tests/`
Expected: 0 errors / 0 warnings

- [ ] **Step 3: 全量测试**

Run: `cd client && npx vitest run`
Expected: PASS

- [ ] **Step 4: commit + push**

```bash
git push origin v0.6.x-dev
```

---

## 总结

- **Tasks**: 7 个
- **修改文件**: ~10
- **新增测试**: ~15
- **核心改动**: affixPools 扩展 + Affix.minSubCategory + getEquipmentResistances/getEquipmentMPBonus + 战斗初始化合并
