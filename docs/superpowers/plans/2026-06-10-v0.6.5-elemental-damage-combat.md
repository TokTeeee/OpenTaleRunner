# v0.6.5 元素伤害接入战斗 + 元素 Icon 表现 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 武器元素伤害词条接入 resolveAttack 伤害计算 + 战斗日志元素信息附带 icon

**Architecture:** ActionResolver 新增 getWeaponElementalDamage 提取武器元素词条 → resolveAttack 步骤2.5 计算元素附加伤害(受抗性减免) → 日志 message 中元素名前加 ELEMENT_ICONS

**Tech Stack:** TypeScript (strict) + Vitest

---

## File Structure

### 修改文件

```
client/src/
├── services/combat/ActionResolver.ts    # 新增 getWeaponElementalDamage + resolveAttack 元素步骤 + 日志 icon
client/tests/
├── services/combat/ActionResolver.test.ts  # 新增元素伤害测试
```

---

## Task 1: 新增 getWeaponElementalDamage + resolveAttack 元素附加伤害

**Files:**
- Modify: `client/src/services/combat/ActionResolver.ts`

- [ ] **Step 1: 新增 getWeaponElementalDamage 辅助函数**

在 `getEquipmentMPBonus` 函数之后追加:

```typescript
/** 从武器 effects 提取元素附加伤害 (elemental_damage 词条). 一把武器最多1个元素词条. */
export function getWeaponElementalDamage(
  item: { effects?: { type: string; value?: number | string | Record<string, unknown> }[] } | null | undefined,
): { element: keyof ElementalResistances; bonus: number } | null {
  if (!item?.effects) return null;
  for (const eff of item.effects) {
    if (eff.type === 'elemental_damage' && typeof eff.value === 'object' && eff.value !== null) {
      const v = eff.value as Record<string, unknown>;
      for (const [element, val] of Object.entries(v)) {
        if (typeof val === 'number' && val > 0) {
          return { element: element as keyof ElementalResistances, bonus: val };
        }
      }
    }
  }
  return null;
}
```

- [ ] **Step 2: 修改 resolveAttack，在步骤2(伤害计算)和步骤3(实际生效)之间插入元素附加伤害**

在 resolveAttack 中，找到步骤3的注释 `// 步骤3: 实际生效`，在它之前插入:

```typescript
    // 步骤2.5: 武器元素附加伤害
    let elementalDamage = 0;
    let weaponElement: keyof ElementalResistances | null = null;
    const weaponElem = getWeaponElementalDamage(attacker.equipped.weapon);
    if (weaponElem) {
      weaponElement = weaponElem.element;
      const resistValue = target.elementalResistances[weaponElem.element] ?? 0;
      elementalDamage = Math.max(0, Math.round(weaponElem.bonus * (1 - resistValue / 100)));
      log.push({
        kind: 'action',
        round: state.round,
        turn: state.turn,
        message: `  ${ELEMENT_ICONS[weaponElem.element]} ${ELEMENT_LABELS[weaponElem.element]}属性附加: ${weaponElem.bonus}×(1-${resistValue}/100)=${elementalDamage}`,
        data: { elementalBonus: weaponElem.bonus, element: weaponElem.element, resistValue, elementalDamage },
        timestamp: Date.now(),
      });
    }
```

然后修改步骤3中的伤害生效，将元素附加伤害加到总伤害中:

找到:
```typescript
    // 步骤3: 实际生效
    const targetHpBefore = target.hp;
    useCombatStore.getState().applyDamage(target.id, damage.total);
    log.push({
      kind: 'action',
      round: state.round,
      turn: state.turn,
      message: `  ${target.name} 受到 ${damage.total} 伤害 (HP ${targetHpBefore}→${targetHpBefore - damage.total})`,
      timestamp: Date.now(),
    });
```

替换为:
```typescript
    // 步骤3: 实际生效
    const totalDamage = damage.total + elementalDamage;
    const targetHpBefore = target.hp;
    useCombatStore.getState().applyDamage(target.id, totalDamage);
    const elementLabel = weaponElement ? ` ${ELEMENT_ICONS[weaponElement]}${ELEMENT_LABELS[weaponElement]}` : '';
    log.push({
      kind: 'action',
      round: state.round,
      turn: state.turn,
      message: `  ${target.name} 受到 ${damage.total}${elementalDamage > 0 ? `+${elementalDamage}${elementLabel}` : ''} 伤害 (HP ${targetHpBefore}→${targetHpBefore - totalDamage})`,
      data: { physicalDamage: damage.total, elementalDamage, totalDamage, element: weaponElement },
      timestamp: Date.now(),
    });
```

同时修改击杀判定，将 `damage.total` 改为 `totalDamage`:

找到:
```typescript
    if (targetHpBefore - damage.total <= 0) {
```

替换为:
```typescript
    if (targetHpBefore - totalDamage <= 0) {
```

- [ ] **Step 3: 修改 resolveAbilityDamage 日志，元素名前加 icon**

在 resolveAbilityDamage 中，找到最终伤害行:

```typescript
      message: `  ${target.name} 受到 ${finalDamage} ${element ? `${ELEMENT_LABELS[element]}` : ''}伤害 (HP ${targetHpBefore}→${targetHpBefore - finalDamage})`,
```

替换为:

```typescript
      message: `  ${target.name} 受到 ${finalDamage} ${element ? `${ELEMENT_ICONS[element]}${ELEMENT_LABELS[element]}` : ''}伤害 (HP ${targetHpBefore}→${targetHpBefore - finalDamage})`,
```

找到抗性行:

```typescript
        message: `  抗性: ${preResistance}×(1-${resistanceValue}/100) ${resistanceValue > 0 ? `-${reduction}` : `+${Math.abs(reduction)}`}${resistanceValue > 0 ? '(抗性减免)' : '(弱点增伤)'}=${postResistance}`,
```

替换为:

```typescript
        message: `  ${ELEMENT_ICONS[element]} 抗性: ${preResistance}×(1-${resistanceValue}/100) ${resistanceValue > 0 ? `-${reduction}` : `+${Math.abs(reduction)}`}${resistanceValue > 0 ? '(抗性减免)' : '(弱点增伤)'}=${postResistance}`,
```

- [ ] **Step 4: typecheck**

Run: `cd client && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 5: commit**

```bash
git add client/src/services/combat/ActionResolver.ts
git commit -m "feat(v0.6.5): resolveAttack 元素附加伤害 + 战斗日志元素 icon"
```

---

## Task 2: ActionResolver 元素伤害测试

**Files:**
- Modify: `client/tests/services/combat/ActionResolver.test.ts`

- [ ] **Step 1: 新增 getWeaponElementalDamage 单测**

在 ActionResolver.test.ts 文件中，在 import 区域追加:

```typescript
import { getWeaponElementalDamage } from '../../../src/services/combat/ActionResolver';
```

在文件末尾（最后一个 describe 块之后）追加:

```typescript
describe('getWeaponElementalDamage', () => {
  it('从武器 effects 提取元素附加伤害', () => {
    const weapon = {
      effects: [
        { type: 'damage_bonus', value: 4 },
        { type: 'elemental_damage', value: { fire: 3 } },
      ],
    };
    const result = getWeaponElementalDamage(weapon);
    expect(result).toEqual({ element: 'fire', bonus: 3 });
  });

  it('无 elemental_damage 词条返回 null', () => {
    const weapon = {
      effects: [{ type: 'damage_bonus', value: 4 }],
    };
    expect(getWeaponElementalDamage(weapon)).toBeNull();
  });

  it('null/undefined 武器返回 null', () => {
    expect(getWeaponElementalDamage(null)).toBeNull();
    expect(getWeaponElementalDamage(undefined)).toBeNull();
  });

  it('空 effects 返回 null', () => {
    expect(getWeaponElementalDamage({ effects: [] })).toBeNull();
  });
});
```

- [ ] **Step 2: 新增 resolveAttack 元素附加伤害集成测试**

在文件末尾追加:

```typescript
describe('resolveAttack 元素附加伤害', () => {
  it('武器有火属性词条时, 伤害包含元素附加 (受抗性减免)', () => {
    const state = makeCombatState({
      attacker: { ...makeCombatant('a1', '玩家', 'player'), equipped: { ...makeCombatant('a1', '玩家', 'player').equipped, weapon: { name: '火焰剑', effects: [{ type: 'damage_bonus', value: 4 }, { type: 'elemental_damage', value: { fire: 5 } }] } } },
      target: { ...makeCombatant('t1', '哥布林', 'enemy'), elementalResistances: { ...ZERO_RESISTANCES, fire: 40 } },
    });
    const resolver = new ActionResolver({ roll: makeConstRoll([15, 3]), damageScale: 0 });
    const result = resolver.resolve({ kind: 'attack', attackerId: 'a1', targetId: 't1' }, state);
    // 日志中应有火属性附加行
    const elemLog = result.log.find((l) => l.message.includes('火属性附加'));
    expect(elemLog).toBeTruthy();
    // 火附加 5 × (1-40/100) = 3
    expect(elemLog!.message).toContain('3');
    // 最终伤害行应包含元素伤害
    const dmgLog = result.log.find((l) => l.message.includes('受到') && l.message.includes('伤害'));
    expect(dmgLog).toBeTruthy();
  });

  it('武器无元素词条时, 无元素附加伤害', () => {
    const state = makeCombatState({
      attacker: makeCombatant('a1', '玩家', 'player'),
      target: makeCombatant('t1', '哥布林', 'enemy'),
    });
    const resolver = new ActionResolver({ roll: makeConstRoll([15, 3]), damageScale: 0 });
    const result = resolver.resolve({ kind: 'attack', attackerId: 'a1', targetId: 't1' }, state);
    const elemLog = result.log.find((l) => l.message.includes('属性附加'));
    expect(elemLog).toBeUndefined();
  });
});
```

注意: 上面的 `makeCombatState`, `makeCombatant`, `makeConstRoll`, `ZERO_RESISTANCES` 需要确认是否已在测试文件中定义。如果 `makeCombatant` 不支持 `equipped.weapon` 为带 effects 的对象，需要调整工厂函数。

- [ ] **Step 3: 运行测试**

Run: `cd client && npx vitest run tests/services/combat/ActionResolver.test.ts`
Expected: PASS

如果测试因工厂函数不支持 weapon effects 而失败，调整测试代码使其兼容现有工厂函数结构。

- [ ] **Step 4: commit**

```bash
git add client/tests/services/combat/ActionResolver.test.ts
git commit -m "test(v0.6.5): getWeaponElementalDamage + resolveAttack 元素附加伤害测试"
```

---

## Task 3: 全量验证

**Files:** (无)

- [ ] **Step 1: typecheck**

Run: `cd client && npx tsc --noEmit`
Expected: 0 errors

- [ ] **Step 2: 全量测试**

Run: `cd client && npx vitest run`
Expected: PASS

- [ ] **Step 3: commit + push**

```bash
git push origin v0.6.x-dev
```

---

## 总结

- **Tasks**: 3 个
- **修改文件**: 2 (ActionResolver.ts, ActionResolver.test.ts)
- **核心改动**: getWeaponElementalDamage + resolveAttack 元素附加步骤 + 日志 ELEMENT_ICONS
