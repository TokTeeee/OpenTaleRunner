# v0.5.8 — 收尾 PR 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 1 个 PR 收尾 v0.5.x 全部遗留 (3 件小事): F 文档清理 + A-5 删孤儿 action + A-1 补 e2e 验当前 retry 行为

**Architecture:**
- **F (CacheManager)**: 仅删 en/zh `Client-Architecture.md` 各 3 处 stale 描述,不动代码 (CacheManager.ts 在 v0.3 已删)
- **A-5 (applyServerAttributeSpend)**: 删 action + type + 1 个测试,纯净化
- **A-1 (multi-PATCH retry e2e)**: 新建 `client/tests/integration/expRetry.test.tsx`, 1 个 `it` 跑 11 个 step,验"失败 PATCH 不写 store + 后续事件仍能触发新 PATCH"

**Tech Stack:** vitest, @testing-library/react, zustand stores, EventBus singleton, vi.spyOn(fetch)

**Spec:** `docs/superpowers/specs/2026-06-07-v0.5.8-cleanup-design.md` (commit `ae73e90`)

**继承自:** v0.5.7 (`d0042b5` + tag `v0.5.7`) 的 parking lot 残留.

---

## File Structure

**Create:**
- `client/tests/integration/expRetry.test.tsx` — A-1 e2e 测试 (~100 行)

**Modify:**
- `docs/en/Client-Architecture.md` — F 删 3 处 (line 112, 209, 217)
- `docs/zh/客户端架构与机制.md` — F 删 3 处 (line 112, 211, 219)
- `client/src/stores/characterStore.ts` — A-5 删 action (line 226-236) + type (line 59)
- `client/tests/stores/characterStore_v051.test.ts` — A-5 删 1 个 describe 块 (line 73-86)
- `CHANGELOG.md` — 加 v0.5.8 段

**Do NOT touch:**
- `client/src/services/level/subscribeCharacterEvents.ts` — A-1 决策:**不动 retry 逻辑**
- `client/src/services/event/**` — 不动
- `client/tests/integration/levelUpFlow.test.tsx` — 不动
- 任何其他 v0.5.x 模块

---

## Task 1: F — 删 en Client-Architecture.md 3 处 CacheManager 残留

**Files:**
- Modify: `docs/en/Client-Architecture.md`

- [ ] **Step 1: 读现状确认 line 号**

Read: `docs/en/Client-Architecture.md` line 110-115, 207-212, 215-220

确认 3 处位置:
- L112: "5. `CacheManager.ts` remains a placeholder system — a decision is needed on formal integration or cleanup."
- L209: 架构表 "Cache Manager" 行
- L217: "5. `CacheManager.ts`, auth token ownership, ..."

- [ ] **Step 2: 删 L112 (5. 已知限制段第 5 条)**

把:
```
5. `CacheManager.ts` remains a placeholder system — a decision is needed on formal integration or cleanup.
```
替换为: `(空字符串)`, 即整条删。

注意保留前 4 条 + 后续条目编号自动不变 (不重排)。

- [ ] **Step 3: 删 L209 架构表 "Cache Manager" 行**

把:
```
| Cache Manager | `client/src/services/cache/CacheManager.ts` | Provides generic caching capability | Not yet integrated | Placeholder system; decision needed on integration or cleanup |
```
整行删 (含换行)。

- [ ] **Step 4: 删 L217 "5. 后续可补强点" 段的 CacheManager 字段**

把:
```
5. `CacheManager.ts`, auth token ownership, and the unification approach for multiplayer API vs. generic API are the most apparent architectural improvement targets.
```
替换为:
```
5. Auth token ownership and the unification approach for multiplayer API vs. generic API are the most apparent architectural improvement targets.
```

- [ ] **Step 5: 跑 grep 验证无残留**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "CacheManager" docs/en/Client-Architecture.md`

Expected: 0 matches.

- [ ] **Step 6: Commit**

```bash
cd "f:\OpenTaleRunner"
git add docs/en/Client-Architecture.md
git commit -m "docs(en): v0.5.8 — drop stale CacheManager placeholder references

CacheManager.ts was removed in v0.3 (CHANGELOG line 465) but
en/Client-Architecture.md still lists it as 'placeholder system
requiring decision'. Sync the docs with the actual codebase state.

Refs: v0.5.8 spec §2.1 (F item)."
```

---

## Task 2: F — 删 zh 客户端架构与机制.md 3 处 CacheManager 残留

**Files:**
- Modify: `docs/zh/客户端架构与机制.md`

- [ ] **Step 1: 读现状确认 line 号**

Read: `docs/zh/客户端架构与机制.md` line 110-115, 209-213, 217-221

确认 3 处位置:
- L112: "5. `CacheManager.ts` 仍是预留型系统,需决定正式集成或清理。"
- L211: 架构表"缓存管理器"行
- L219: "5. `CacheManager.ts`、认证 token 归属、..."

- [ ] **Step 2: 删 L112**

把:
```
5. `CacheManager.ts` 仍是预留型系统，需决定正式集成或清理。
```
整条删。

- [ ] **Step 3: 删 L211 架构表行**

把:
```
| 缓存管理器 | `client/src/services/cache/CacheManager.ts` | 提供通用缓存能力 | 暂未集成 | 预留系统,后续需决定接入或清理 |
```
整行删。

- [ ] **Step 4: 删 L219 字段**

把:
```
5. `CacheManager.ts`、认证 token 归属、联机 API 与通用 API 的统一方式,是当前最明显的架构补强点。
```
替换为:
```
5. 认证 token 归属、联机 API 与通用 API 的统一方式,是当前最明显的架构补强点。
```

- [ ] **Step 5: 跑 grep 验证无残留**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "CacheManager" docs/zh/客户端架构与机制.md`

Expected: 0 matches.

- [ ] **Step 6: Commit**

```bash
cd "f:\OpenTaleRunner"
git add docs/zh/客户端架构与机制.md
git commit -m "docs(zh): v0.5.8 — drop stale CacheManager placeholder references

镜像 en 同步清理. CacheManager.ts 已在 v0.3 删除, 文档残留.
Refs: v0.5.8 spec §2.1 (F item)."
```

---

## Task 3: A-5 — 删 characterStore.ts action + type

**Files:**
- Modify: `client/src/stores/characterStore.ts`

- [ ] **Step 1: 读现状确认 line 号**

Read: `client/src/stores/characterStore.ts` line 55-65 (type 块) 和 line 222-240 (实现块)。

确认:
- type L59: `applyServerAttributeSpend: (patch: { attributes: Attributes; unspentAttributePoints: number }) => void;`
- impl L226-236: `applyServerAttributeSpend: (patch) => set((s) => { ... }),`

- [ ] **Step 2: 删 type 声明 (L59)**

把:
```ts
  applyServerAttributeSpend: (patch: { attributes: Attributes; unspentAttributePoints: number }) => void;
```
这一行删 (含尾部换行)。

注意检查 `Attributes` 类型是否仅此一处导入 — 如果是,删 type 也要把 `import type { Attributes } from '...'` 删掉。grep 确认:
```
cd "f:\OpenTaleRunner\client" && grep -c "Attributes" src/stores/characterStore.ts
```
如果 > 1,保留 import;如果 = 1,删 import。

(预计 > 1, 因为 `attributes: { ... }` 在初始 state 也用 Attributes 类型。)

- [ ] **Step 3: 删 action 实现 (L226-236)**

把:
```ts
  applyServerAttributeSpend: (patch) =>
    set((s) => {
      if (!s.character) return s;
      return {
        character: {
          ...s.character,
          attributes: patch.attributes,
          unspentAttributePoints: patch.unspentAttributePoints,
        },
      };
    }),

```
整个块删 (含前面的缩进和尾部 `,`)。

注意保留上下文 — 前 `applyServerExpGrant` 块结尾的 `,` 不动,后 `setClass` 块开头的注释保留。

- [ ] **Step 4: 跑 typecheck 验证**

Run: `cd "f:\OpenTaleRunner\client" && npm run typecheck 2>&1 | Select-Object -Last 5`

Expected: exit 0 (无 error)。

- [ ] **Step 5: 跑 lint 验证**

Run: `cd "f:\OpenTaleRunner\client" && npm run lint 2>&1 | Select-Object -Last 5`

Expected: exit 0 (无 warning/error)。

- [ ] **Step 6: Commit**

```bash
cd "f:\OpenTaleRunner"
git add client/src/stores/characterStore.ts
git commit -m "refactor(characterStore): v0.5.8 — drop orphan applyServerAttributeSpend

v0.5.6 parking lot item: UI consumer AttributeRow was deleted in
v0.5.6, leaving this action with no callers. Per v0.5.8 spec §2.2
user decision: delete the action + its type declaration. Will be
re-introduced when a v0.6+ spec reintroduces the attribute-spend UI.

Refs: v0.5.8 spec §2.2 (A-5 item)."
```

---

## Task 4: A-5 — 删 characterStore_v051.test.ts 1 个 describe 块

**Files:**
- Modify: `client/tests/stores/characterStore_v051.test.ts`

- [ ] **Step 1: 读现状**

已在上文确认 (line 73-86 是被删的 describe 块)。

- [ ] **Step 2: 删 describe 块 (L73-86)**

把:
```ts
describe('characterStore — applyServerAttributeSpend', () => {
  beforeEach(() => useCharacterStore.setState({ character: makeChar({ unspentAttributePoints: 3 }), isLoaded: true }));

  it('applies new attributes and decrements pool', () => {
    const newAttrs = { ...BASE_ATTRS, STR: 11 };
    useCharacterStore.getState().applyServerAttributeSpend({
      attributes: newAttrs,
      unspentAttributePoints: 2,
    });
    const c = useCharacterStore.getState().character!;
    expect(c.attributes.STR).toBe(11);
    expect(c.unspentAttributePoints).toBe(2);
  });
});
```
整块删。

- [ ] **Step 3: 检查 BASE_ATTRS 是否仍被用**

如果删完 BASE_ATTRS 没被引用,删 `const BASE_ATTRS: Attributes = ...` 声明 (L5)。grep:
```
cd "f:\OpenTaleRunner\client" && grep -c "BASE_ATTRS" tests/stores/characterStore_v051.test.ts
```
- = 1: 只剩 import/声明,删 `const BASE_ATTRS: Attributes = { STR: 10, DEX: 10, CON: 10, INT: 10, WIS: 10, CHA: 10 };`
- > 1: 保留 (因为 v0.5.1 那 3 个 it 可能用不到它,删后这个 const 真成死代码)

(预计 = 1, 删掉)

- [ ] **Step 4: 跑测试**

Run: `cd "f:\OpenTaleRunner\client" && npx vitest run tests/stores/characterStore_v051.test.ts 2>&1 | Select-String -Pattern "passed|failed"`

Expected: "4 passed (4)" — 原来 5 个 it (v0.5.1 clamp ×3 + applyServerExpGrant ×2), 删 1 个剩 4。

- [ ] **Step 5: 跑全 client 测试**

Run: `cd "f:\OpenTaleRunner\client" && npm run test:run 2>&1 | Select-Object -Last 5`

Expected: 85/85 文件, 938/938 tests (939 - 1 = 938)。

- [ ] **Step 6: Commit**

```bash
cd "f:\OpenTaleRunner"
git add client/tests/stores/characterStore_v051.test.ts
git commit -m "test(characterStore): v0.5.8 — drop applyServerAttributeSpend test

Mirrors the action removal in the previous commit. v0.5.8 spec
§2.2 (A-5 item)."
```

---

## Task 5: A-1 — 写 expRetry.test.tsx 骨架 + 编译验证

**Files:**
- Create: `client/tests/integration/expRetry.test.tsx`

- [ ] **Step 1: 写最小骨架**

Create `client/tests/integration/expRetry.test.tsx` with:

```tsx
/**
 * v0.5.8 — subscribeCharacterExpEvents 失败链路 e2e
 *
 * 覆盖 spec §2.3 A-1 item:
 *   - PATCH /exp 失败 (非 2xx) 时不写 store
 *   - 失败后 emit 新事件, debounce 后能触发新的 PATCH
 *   - 新的 PATCH 成功时, applyServerExpGrant 正常写入
 *
 * **不** 覆盖 (留 v0.6 spec):
 *   - 真 retry 队列: 当前 flush() 在 fetch 前把 pending 清零,
 *     失败时 amount 直接丢弃, 不回填。要测 retry 队列合并
 *     需先改 subscribeCharacterEvents.ts:49-87 行为, 不在本 PR 范围。
 */
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import { eventBus } from '../../src/services/event/EventBus';
import { EVENTS } from '../../src/services/event/events';
import { useCharacterStore } from '../../src/stores/characterStore';
import { useCombatStore } from '../../src/stores/combatStore';
import { useAuthStore } from '../../src/stores/authStore';
import { useSettingsStore } from '../../src/stores/settingsStore';
import { subscribeCharacterExpEvents } from '../../src/services/level/subscribeCharacterEvents';
import type { Character } from '../../src/types/character';
import { resetClientStores } from '../utils/resetStores';

function makeChar(level: number): Character {
  return {
    characterId: 'c1',
    playerId: 'p1',
    name: 'Test',
    race: '人类',
    background: '',
    appearance: '',
    attributes: { STR: 10, DEX: 10, CON: 10, INT: 10, WIS: 10, CHA: 10 },
    skills: [],
    inventory: { items: [], equipped: { weapon: null, armor: null, accessory: null }, currency: { gold: 0, silver: 0, copper: 0 } },
    hp: 20,
    maxHp: 20,
    vital: { hunger: 0, thirst: 0, fatigue: 0, hygiene: 0, morale: 0, wound: 0, temperature: 37, encumbrance: 0 },
    reputation: { goodness: 0, violence: 0, lawfulness: 0, regional: {} },
    conditions: [],
    joinedRegion: 'r',
    joinedWorldDay: 1,
    currentLocalDay: 1,
    lastActionTime: '',
    recentHistory: [],
    level,
    exp: 0,
    expToNext: 100,
    unspentAttributePoints: 0,
    classId: null,
    classSkills: [],
  };
}

beforeEach(() => {
  resetClientStores();
  useCombatStore.setState({ phase: 'idle', isPlayerTurn: false, active: false } as any);
  useAuthStore.setState({ token: 'tk' });
  useSettingsStore.setState({ server: { endpoint: 'http://api.test' } } as never);
});

afterEach(() => {
  eventBus.clear();
  vi.restoreAllMocks();
});

describe('v0.5.8 — EXP PATCH failure path', () => {
  it('skeleton: placeholder', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 2: 跑测试**

Run: `cd "f:\OpenTaleRunner\client" && npx vitest run tests/integration/expRetry.test.tsx 2>&1 | Select-String -Pattern "passed|failed"`

Expected: "1 passed (1)" — 编译过 + placeholder 过。

- [ ] **Step 3: Commit**

```bash
cd "f:\OpenTaleRunner"
git add client/tests/integration/expRetry.test.tsx
git commit -m "test(integration): v0.5.8 — add expRetry.test.tsx skeleton"
```

---

## Task 6: A-1 — 替换 placeholder 为 11 步剧本

**Files:**
- Modify: `client/tests/integration/expRetry.test.tsx`

- [ ] **Step 1: 替换 `it('skeleton: ...')` 为完整剧本**

把:
```tsx
  it('skeleton: placeholder', () => {
    expect(1 + 1).toBe(2);
  });
```

替换为:
```tsx
  it('PATCH /exp 失败时不写 store, 后续事件仍能触发新 PATCH', async () => {
    // ---------------------------------------------------------------------
    // Step 2: 准备 mock server — 第一次返 503, 第二次返 200
    // ---------------------------------------------------------------------
    let fetchCalls = 0;
    const fetchMock = vi.fn(async (url: string, init: RequestInit) => {
      fetchCalls += 1;
      if (url.endsWith('/exp') && init.method === 'PATCH') {
        if (fetchCalls === 1) {
          return new Response('{"error":"rate_limited"}', {
            status: 503,
            headers: { 'Content-Type': 'application/json' },
          });
        }
        // 第二次: 成功
        return new Response(
          JSON.stringify({ level: 2, exp: 0, expToNext: 283, unspentAttributePoints: 0 }),
          { status: 200, headers: { 'Content-Type': 'application/json' } },
        );
      }
      throw new Error(`unexpected fetch call: ${init.method} ${url}`);
    });
    vi.spyOn(globalThis, 'fetch').mockImplementation(fetchMock as unknown as typeof fetch);

    // ---------------------------------------------------------------------
    // Step 0: 角色 L1 写入 store
    // ---------------------------------------------------------------------
    useCharacterStore.setState({ character: makeChar(1), isLoaded: true });
    expect(useCharacterStore.getState().character!.level).toBe(1);

    // ---------------------------------------------------------------------
    // Step 1: 注册 EXP 订阅者 (debounce 150ms 加速)
    // ---------------------------------------------------------------------
    const unsub = subscribeCharacterExpEvents({ debounceMs: 150 });

    // ---------------------------------------------------------------------
    // Step 3: emit COMBAT_KILL (5 exp) — 期望 PATCH /exp 失败
    // ---------------------------------------------------------------------
    eventBus.emit(EVENTS.COMBAT_KILL, { killerId: 'p1', victimId: 'e1' });

    // ---------------------------------------------------------------------
    // Step 4: 等 debounce 窗口 + 一次 microtask flush
    // ---------------------------------------------------------------------
    await new Promise((r) => setTimeout(r, 250));

    // ---------------------------------------------------------------------
    // Step 5: 验 fetch 被叫 1 次, 第 1 次 PATCH /exp 返 503
    // ---------------------------------------------------------------------
    expect(fetchCalls).toBe(1);
    const [url1, init1] = (fetchMock.mock.calls[0] as [string, RequestInit]);
    expect(url1).toBe('http://api.test/api/v1/characters/c1/exp');
    expect(init1.method).toBe('PATCH');
    expect(JSON.parse(init1.body as string)).toEqual({ amount: 5, difficulty: 'normal' });

    // ---------------------------------------------------------------------
    // Step 6: 验失败不写 store (level 仍 1, 失败 amount 被丢弃)
    // ---------------------------------------------------------------------
    const c1 = useCharacterStore.getState().character!;
    expect(c1.level).toBe(1);
    expect(c1.exp).toBe(0);

    // ---------------------------------------------------------------------
    // Step 7: emit COMBAT_HIT ×3 (3 exp) — 期望新 PATCH /exp 成功
    // ---------------------------------------------------------------------
    for (let i = 0; i < 3; i += 1) {
      eventBus.emit(EVENTS.COMBAT_HIT, { attackerId: 'p1', targetId: 'e1', damage: 7, isCrit: false });
    }

    // ---------------------------------------------------------------------
    // Step 8: 等 debounce 窗口
    // ---------------------------------------------------------------------
    await new Promise((r) => setTimeout(r, 250));

    // ---------------------------------------------------------------------
    // Step 9: 验 fetch 被叫 2 次, 第二次 PATCH /exp 返 200, body amount=3
    // ---------------------------------------------------------------------
    expect(fetchCalls).toBe(2);
    const [url2, init2] = (fetchMock.mock.calls[1] as [string, RequestInit]);
    expect(url2).toBe('http://api.test/api/v1/characters/c1/exp');
    expect(init2.method).toBe('PATCH');
    expect(JSON.parse(init2.body as string)).toEqual({ amount: 3, difficulty: 'normal' });

    // ---------------------------------------------------------------------
    // Step 10: 验 applyServerExpGrant 写入 (level === 2)
    // ---------------------------------------------------------------------
    const c2 = useCharacterStore.getState().character!;
    expect(c2.level).toBe(2);
    expect(c2.exp).toBe(0);
    expect(c2.expToNext).toBe(283);

    // ---------------------------------------------------------------------
    // Step 11: cleanup
    // ---------------------------------------------------------------------
    unsub();
  });
```

- [ ] **Step 2: 跑测试, 期望 PASS**

Run: `cd "f:\OpenTaleRunner\client" && npx vitest run tests/integration/expRetry.test.tsx 2>&1 | Select-Object -First 30`

Expected: "1 passed (1)"。

如果失败, 常见原因:
- `Cannot find name 'RequestInit'` → 顶部加 `/// <reference lib="dom" />` (与 v0.5.7 plan 同样的 fallback)
- `fetchCalls === 1` 拿到 0 → debounce 没 flush, 加 `await new Promise((r) => setTimeout(r, 500))` 排查
- `c2.level === 2` 拿到 1 → mock 第二次没返 200, 检查 `fetchCalls === 1` 判断
- `applyServerExpGrant` 类型错 → 看 `client/src/stores/characterStore.ts` 的 signature, 当前是 `(patch: { level, exp, expToNext, unspentAttributePoints }) => void`

- [ ] **Step 3: 跑 10 次, 验证 0 flaky**

Run (PowerShell):
```powershell
cd "f:\OpenTaleRunner\client"
$pass=0; $fail=0
for ($i=1; $i -le 10; $i++) {
  $out = npx vitest run tests/integration/expRetry.test.tsx 2>&1 | Out-String
  if ($out -match "1 passed") { $pass++; Write-Output "Run $i`: PASS" }
  else { $fail++; Write-Output "Run $i`: FAIL" }
}
Write-Output "---"
Write-Output "PASS=$pass FAIL=$fail"
```

Expected: 10/10 PASS。

- [ ] **Step 4: 跑全 client 测试**

Run: `cd "f:\OpenTaleRunner\client" && npm run test:run 2>&1 | Select-Object -Last 5`

Expected: 86/86 文件 (85+1), 939/939 tests (938+1, 因 A-5 删 1 个 + A-1 加 1 个 → 净 0, 但实际 A-1 加 1 抵消 A-5 删 1 → 938 + 0 = 938? 实际 939 - 1 + 1 = 939)。

不对, 重新算:
- v0.5.7 终态: 939 tests
- A-5 删 1 个 (applyServerAttributeSpend) → 938
- A-1 加 1 个 it (expRetry) → 939
- 终态: 939 tests

(预期 86 文件, 939 tests, 跟 v0.5.7 一样。)

- [ ] **Step 5: 跑 typecheck + lint**

Run:
```powershell
cd "f:\OpenTaleRunner\client"; npm run typecheck 2>&1 | Select-Object -Last 5; Write-Output "---lint---"; npm run lint 2>&1 | Select-Object -Last 5
```

Expected: 两个都 exit 0。

- [ ] **Step 6: Commit**

```bash
cd "f:\OpenTaleRunner"
git add client/tests/integration/expRetry.test.tsx
git commit -m "test(integration): v0.5.8 — EXP PATCH failure path e2e

单一剧本式 it() 走完 11 步, 验当前 subscribeCharacterExpEvents
的真实行为:
  Step 0:  character L1 写入
  Step 1:  register subscriber (debounce 150ms)
  Step 2:  fetch mock: 第1次返 503, 第2次返 200
  Step 3:  emit COMBAT_KILL (5 exp)
  Step 4:  await debounce
  Step 5:  验 fetch 被叫 1 次, PATCH /exp 返 503, body amount=5
  Step 6:  验失败不写 store (level 仍 1, 失败 amount 丢弃)
  Step 7:  emit COMBAT_HIT ×3 (3 exp)
  Step 8:  await debounce
  Step 9:  验 fetch 被叫 2 次, 第 2 次 body amount=3
  Step 10: 验 applyServerExpGrant 写入 → level=2
  Step 11: cleanup

不覆盖 (留 v0.6): 真 retry 队列 — 当前 flush() 在 fetch 前把
pending 清零, 失败时 amount 直接丢弃, 不回填。要测 retry 队列
合并需先改 subscribeCharacterEvents.ts:49-87 行为, 不在本 PR 范围。

补 v0.5.7 parking lot 第 1 项 'multi-PATCH failure / retry chain'。
连跑 10/10 稳定. typecheck + lint: 0 error."
```

---

## Task 7: CHANGELOG v0.5.8 + 整体跑测 + tag

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 在 CHANGELOG 顶部加 v0.5.8 段**

在 `# Changelog\n\n## v0.5.7 — ...` 之前插入:

```markdown
## v0.5.8 — 2026-06-07 (v0.5.x 收尾 — 文档 / 孤儿 action / 失败链路 e2e)

Closes the last v0.5.x parking lot items in one small PR. No engine /
combat code changes; no public API changes for end users.

### What's in this release

**F — CacheManager doc cleanup**

`CacheManager.ts` was actually removed in v0.3 (see CHANGELOG line
465), but `docs/en/Client-Architecture.md` and
`docs/zh/客户端架构与机制.md` still listed it as a "placeholder
system requiring decision". Removed the 6 stale references (3 per
file) so the architecture docs match the actual codebase.

**A-5 — Drop orphan `applyServerAttributeSpend`**

v0.5.6 parking lot item: the only UI consumer (`AttributeRow`) was
deleted in v0.5.6, leaving `characterStore.applyServerAttributeSpend`
with no callers. Per v0.5.8 spec §2.2 user decision, removed the
action + its type declaration + its 1 test. Will be re-introduced
when a v0.6+ spec reintroduces the attribute-spend UI.

**A-1 — EXP PATCH failure-path e2e**

Added `client/tests/integration/expRetry.test.tsx`, a single-script
`it()` that walks the failure path of `subscribeCharacterExpEvents`:
assert that a non-2xx PATCH `/exp` does **not** write the store, and
that a subsequent event still triggers a fresh PATCH that does
write. Closes v0.5.7 parking lot item 1.

**Important: this test does NOT verify a true retry queue.**
The current implementation (`subscribeCharacterEvents.ts:49-87`)
clears `pending` *before* `fetch` runs, so a failed PATCH discards
the accumulated amount; only new events can re-trigger a PATCH. The
test asserts this current behavior, not a stronger "merge-on-retry"
behavior. Real retry queue logic is deferred to a v0.6 spec.

### Test results

- **Client**: 10/10 stable runs of the new test; full client suite
  86/86 files, 939/939 tests pass (A-5 −1 + A-1 +1 = net 0).
- **typecheck + lint**: 0 errors.
- **Server**: not touched; 51/51 still pass.

### Out of scope (parking lot, remaining after this release)

- real server end-to-end (would need a server process in CI)
- multiplayer EXP sharing (v0.6)
- L1 → L20 (3 more tier picks)
- **true retry queue for failed EXP PATCH** (added by A-1; the e2e
  documents current "discard + re-emit" behavior, not a fix)

---

## v0.5.7 — 2026-06-06 (LevelUp end-to-end integration test)
```

- [ ] **Step 2: 整体跑测 + lint + typecheck 验证**

Run:
```powershell
cd "f:\OpenTaleRunner\client"; npm run test:run 2>&1 | Select-Object -Last 5; Write-Output "---typecheck---"; npm run typecheck 2>&1 | Select-Object -Last 5; Write-Output "---lint---"; npm run lint 2>&1 | Select-Object -Last 5
```

Expected:
- test:run → 86/86 文件, 939/939 tests
- typecheck → exit 0
- lint → exit 0

- [ ] **Step 3: Commit + tag**

```bash
cd "f:\OpenTaleRunner"
git add CHANGELOG.md
git commit -m "docs: v0.5.8 changelog"
git tag -a v0.5.8 -m "v0.5.8 — v0.5.x 收尾 PR

1 个 PR 收掉 3 件 v0.5.x 遗留:
- F: en/zh Client-Architecture.md 删 6 处 stale CacheManager 引用
  (实际 v0.3 已删, 文档残留)
- A-5: 删 characterStore.applyServerAttributeSpend 孤儿 action +
  type + 1 测试 (v0.5.6 parking lot)
- A-1: 补 expRetry.test.tsx e2e, 验当前 PATCH /exp 失败行为
  (失败丢弃, 不写 store, 后续事件触发新 PATCH)

不修真 retry 队列 — 留 v0.6 spec.
- 86/86 文件, 939/939 tests
- typecheck + lint: 0 error
- 10/10 跑稳定"
git log --oneline -10
```

- [ ] **Step 4: 验证 tag 存在**

Run: `cd "f:\OpenTaleRunner" && git tag -l v0.5.8`

Expected: 输出 `v0.5.8`。

---

## Self-Review Notes

**Spec coverage check:**
- §1 目标 (3 件事) → Task 1-7 全部覆盖
- §2.1 F (CacheManager 文档) → Task 1 (en) + Task 2 (zh)
- §2.2 A-5 (删 action) → Task 3 (代码) + Task 4 (测试)
- §2.3 A-1 (e2e) → Task 5 (骨架) + Task 6 (11 步剧本)
- §3 文件清单 → Task 1-7 完全对应
- §4 范围外 (retry 队列 + 注释措辞) → 整计划遵守, 不动
- §5 验收 → Task 6 Step 4-5 (10/10 + 全 939) + Task 7 Step 2 (lint+typecheck)
- §6 风险 (A-5 makeChar 影响) → Task 4 Step 1 已查 (无影响)
- §6 风险 (A-1 fetch 503 vs throw) → Task 6 Step 2 fallback 提到
- §6 风险 (F 文档编号) → Task 1-2 Step 2-4 明确"删 1 条, 编号不变"

**Placeholder scan:** 无 "TBD"/"TODO"/"implement later"。

**Type consistency:**
- `Character` 在 Task 5 定义, Task 6 引用 → 一致
- `useCharacterStore.setState({ character, isLoaded: true })` 在 Task 5/6 一致
- `useAuthStore.setState({ token: 'tk' })` + `useSettingsStore.setState({ server: { endpoint: 'http://api.test' } })` 在 Task 5 beforeEach + Task 6 隐式依赖 → 一致
- `subscribeCharacterExpEvents({ debounceMs: 150 })` 在 Task 5/6 一致
- `data-testid` 不需要 (本测试不挂组件)
- `EventName` / `EVENTS.COMBAT_KILL/HIT` 路径在 Step 1 import, Task 6 使用 → 一致

**Risk:**
- **Task 4 Step 3**: `BASE_ATTRS` 在 v0.5.1 clamp 那 3 个 it 里没用到, 删 applyServerAttributeSpend 块后这个 const 100% 死代码, 删掉。
- **Task 6 Step 2**: `RequestInit` 类型可能需要 `/// <reference lib="dom" />` (与 v0.5.7 plan 同样的 fallback)。
- **Task 6 Step 4 测试数算错了吗?** v0.5.7 终态 939; A-5 删 1 = 938; A-1 加 1 = 939 ✓。

**Commit 数量:** 7 个 task, 每个 1 commit, 共 7 commits (Tasks 1+2+3+4+5+6+7)。
