# v0.5.7 — LevelUp 端到端集成测试 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 加 1 个集成测试文件, 单一剧本式 `it()` 走完 v0.5 的 7 个状态跃迁, 作为 v0.5 的"集成回归网"。

**Architecture:** 在 `client/tests/integration/levelUpFlow.test.tsx` 写一个 `describe + it`, 内部用 `// Step N: <desc>` 注释分 12 个 micro-step。事件层用 eventBus 直接 emit (不调真 CombatEngine), fetch 用 `vi.spyOn` 拦截, debounce 调到 200ms 加速。链到 L5+T2 一个 tier 深度, 不动 production 代码。

**Tech Stack:** vitest, @testing-library/react, zustand stores, EventBus singleton.

**Spec:** `docs/superpowers/specs/2026-06-06-v0.5-e2e-levelup-flow-design.md` (commit `e49d5ca`)

**继承自:** v0.5.6 (`a019174`) followup parking lot. 与 `client/tests/integration/guild.test.tsx` 风格一致 (已经有"完整链路"测试但跳过事件层, 用 grantExp 推 level, **不** 验证 event→subscriber→PATCH 链 — 本计划补的是这个空缺)。

---

## File Structure

**Create:**
- `client/tests/integration/levelUpFlow.test.tsx` — 单一集成测试文件 (1 describe + 1 it, ~150 行)

**Modify:**
- `docs/zh/角色系统.md` — 在 v0.5 章末尾加"v0.5.7 端到端回归"小节
- `docs/en/Character-System.md` — 同步翻译
- `CHANGELOG.md` — 顶部加 v0.5.7 段

**Do NOT touch:**
- `client/src/**` — 不改任何 production 代码
- `client/tests/services/level/subscribeCharacterEvents.test.ts` — 不动
- `client/tests/integration/guild.test.tsx` — 不动 (它测的是"已有 level 时"的流程, 本计划测的是"事件驱动升级")

---

## Task 1: 写测试文件骨架 + 编译通过

**Files:**
- Create: `client/tests/integration/levelUpFlow.test.tsx`

- [ ] **Step 1: 写最小骨架, 跑通 "Hello, world" 测试**

在 `client/tests/integration/levelUpFlow.test.tsx` 写入:

```tsx
/**
 * v0.5.7 — LevelUp 端到端集成测试
 *
 * 覆盖 spec 7 个状态跃迁:
 *   战斗 → EXP 事件 → subscriber 聚合 → PATCH /exp → 服务端算新 level
 *   → 写 store → TierUnlockModal 弹 → 玩家选节点 → PATCH /class
 *
 * 与 guild.test.tsx 的区别:
 *   guild.test.tsx 用 grantExp() 推 level, 跳过了事件层; 本测试验的是
 *   event→subscriber→PATCH 契约, 是 v0.5.4 audit Gap #1 的回归网。
 */
import { describe, it, expect, beforeEach, afterEach, vi } from 'vitest';
import React from 'react';
import { render, screen, fireEvent, act } from '@testing-library/react';
import { eventBus } from '../../src/services/event/EventBus';
import { EVENTS } from '../../src/services/event/events';
import { useCharacterStore } from '../../src/stores/characterStore';
import { useCombatStore } from '../../src/stores/combatStore';
import { useAuthStore } from '../../src/stores/authStore';
import { useSettingsStore } from '../../src/stores/settingsStore';
import { subscribeCharacterExpEvents } from '../../src/services/level/subscribeCharacterEvents';
import { GuildClassModal } from '../../src/components/modals/GuildClassModal';
import { TierUnlockModal } from '../../src/components/modals/TierUnlockModal';
import type { Character } from '../../src/types/character';
import { resetClientStores } from '../utils/resetStores';

// TODO: 测试内容将在 Task 2-5 补全

describe('v0.5.7 — LevelUp 端到端链路', () => {
  it('skeleton: placeholder', () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 2: 跑测试, 确认编译通过**

Run: `cd f:\OpenTaleRunner\client && npx vitest run tests/integration/levelUpFlow.test.tsx`

Expected: PASS, "1 passed". (编译能过 = 所有 import 路径都对)

- [ ] **Step 3: Commit**

```bash
cd f:\OpenTaleRunner
git add client/tests/integration/levelUpFlow.test.tsx
git commit -m "test(integration): v0.5.7 — add levelUpFlow.test.tsx skeleton"
```

---

## Task 2: Setup helpers + resetClientStores + character factory

**Files:**
- Modify: `client/tests/integration/levelUpFlow.test.tsx`

- [ ] **Step 1: 把 TODO 替换成 setup 代码**

把 `// TODO: ...` 替换为:

```tsx
// -------------------------------------------------------------------------
// 测试夹具
// -------------------------------------------------------------------------

function makeChar(level: number, classId: string | null, picked: string[]): Character {
  return {
    characterId: 'c1',
    playerId: 'p1',
    name: 'Test',
    race: '人类',
    background: '',
    appearance: '',
    attributes: { STR: 10, DEX: 10, CON: 10, INT: 10, WIS: 10, CHA: 10 },
    skills: [],
    inventory: {
      equipped: { weapon: null, armor: null, accessory: null },
      backpack: [],
      currency: { gold: 0, silver: 0, copper: 0 },
    },
    hp: 20,
    maxHp: 20,
    vital: { hunger: 0, thirst: 0, fatigue: 0, hygiene: 0, morale: 0, wound: 0, temperature: 37, encumbrance: 0 },
    reputation: { goodness: 0, violence: 0, lawfulness: 0, regional: {} },
    conditions: [],
    joinedRegion: 'r',
    joinedWorldDay: 1,
    currentLocalDay: 1,
    lastActionTime: '',
    recentHistory: [],
    level,
    exp: 0,
    expToNext: 100,
    unspentAttributePoints: 0,
    classId,
    classSkills: picked.map((nodeId) => ({ classId: classId || '', nodeId, unlockedAt: 0 })),
  };
}

beforeEach(() => {
  resetClientStores();
  useCombatStore.setState({ phase: 'idle', isPlayerTurn: false, active: false } as any);
  useAuthStore.setState({ token: 'tk' });
  useSettingsStore.setState({ server: { endpoint: 'http://api.test' } } as never);
});

afterEach(() => {
  eventBus.clear();
  vi.restoreAllMocks();
});
```

- [ ] **Step 2: 跑测试确认编译仍过**

Run: `cd f:\OpenTaleRunner\client && npx vitest run tests/integration/levelUpFlow.test.tsx`

Expected: PASS, "1 passed".

- [ ] **Step 3: Commit**

```bash
cd f:\OpenTaleRunner
git add client/tests/integration/levelUpFlow.test.tsx
git commit -m "test(integration): v0.5.7 — add makeChar + beforeEach/afterEach"
```

---

## Task 3: 替换 placeholder it() 为 12 步剧本 (TDD 整体写完, 一次跑过)

**Files:**
- Modify: `client/tests/integration/levelUpFlow.test.tsx`

- [ ] **Step 1: 替换 `it('skeleton: ...')` 为完整剧本**

把整个 `it('skeleton: placeholder', ...)` 替换为:

```tsx
describe('v0.5.7 — LevelUp 端到端链路 (L1→L5+T2)', () => {
  it('战斗 → EXP 事件 → subscriber 聚合 → PATCH /exp → L5 → TierUnlockModal → 选 T2 → PATCH /class', async () => {
    // ---------------------------------------------------------------------
    // 准备 mock server: 路由 /exp 返 L5, 路由 /class 返 ok
    // ---------------------------------------------------------------------
    let expPatchCalls = 0;
    let classPatchCalls = 0;
    const fetchMock = vi.fn(async (url: string, init: RequestInit) => {
      if (url.endsWith('/exp') && init.method === 'PATCH') {
        expPatchCalls += 1;
        return new Response(
          JSON.stringify({ level: 5, exp: 0, expToNext: 600, unspentAttributePoints: 0 }),
          { status: 200, headers: { 'Content-Type': 'application/json' } },
        );
      }
      if (url.endsWith('/class') && init.method === 'PATCH') {
        classPatchCalls += 1;
        return new Response('{"ok":true}', { status: 200, headers: { 'Content-Type': 'application/json' } });
      }
      throw new Error(`unexpected fetch call: ${init.method} ${url}`);
    });
    vi.spyOn(globalThis, 'fetch').mockImplementation(fetchMock as unknown as typeof fetch);

    // ---------------------------------------------------------------------
    // Step 0: 角色 L1, 无职业, 无 classId
    // ---------------------------------------------------------------------
    act(() => {
      useCharacterStore.setState({ character: makeChar(1, null, []), isLoaded: true });
    });
    expect(useCharacterStore.getState().character!.classId).toBeNull();
    expect(useCharacterStore.getState().character!.level).toBe(1);

    // ---------------------------------------------------------------------
    // Step 1: 注册 EXP 订阅者 (debounce 200ms 加速)
    // ---------------------------------------------------------------------
    const unsub = subscribeCharacterExpEvents({ debounceMs: 200 });

    // ---------------------------------------------------------------------
    // Step 2: GuildClassModal 弹出, 选 warrior + T1 → setClass 写入
    // ---------------------------------------------------------------------
    const onCloseGuild = vi.fn();
    const { unmount: unmountGuild } = render(<GuildClassModal open={true} onClose={onCloseGuild} />);
    expect(screen.getByTestId('guild-class-modal')).toBeTruthy();
    fireEvent.click(screen.getByTestId('class-option-warrior'));
    fireEvent.click(screen.getByTestId('t1-node-warrior_t1_1'));
    const c1 = useCharacterStore.getState().character!;
    expect(c1.classId).toBe('warrior');
    expect(c1.classSkills).toHaveLength(1);
    expect(c1.classSkills[0].nodeId).toBe('warrior_t1_1');
    expect(onCloseGuild).toHaveBeenCalledTimes(1);
    // guild PATCH 已经发出 (GuildClassModal 内部 setClass 后异步 PATCH)
    expect(classPatchCalls).toBe(1);

    // ---------------------------------------------------------------------
    // Step 3: emit 战斗事件 12 次 (10 hit + 1 kill + 1 end.victory)
    //          期望: 45 exp 累加, debounce 200ms 后单次 PATCH /exp
    // ---------------------------------------------------------------------
    for (let i = 0; i < 10; i += 1) {
      eventBus.emit(EVENTS.COMBAT_HIT, { attackerId: 'p1', targetId: 'e1', damage: 7, isCrit: false });
    }
    eventBus.emit(EVENTS.COMBAT_KILL, { killerId: 'p1', victimId: 'e1' });
    eventBus.emit(EVENTS.COMBAT_END, { outcome: 'victory' });

    // ---------------------------------------------------------------------
    // Step 4: 等 debounce 窗口 (200ms) + 一次 microtask flush + fetch resolve
    //          留 50ms 余量避免 ci 卡顿 false negative
    // ---------------------------------------------------------------------
    await new Promise((r) => setTimeout(r, 300));

    // ---------------------------------------------------------------------
    // Step 5: 验 PATCH /exp 叫了一次, amount=45, difficulty=normal
    // ---------------------------------------------------------------------
    expect(expPatchCalls).toBe(1);
    const [expUrl, expInit] = (fetchMock.mock.calls[0] as [string, RequestInit]);
    expect(expUrl).toBe('http://api.test/api/v1/characters/c1/exp');
    expect(expInit.method).toBe('PATCH');
    expect(JSON.parse(expInit.body as string)).toEqual({ amount: 45, difficulty: 'normal' });

    // ---------------------------------------------------------------------
    // Step 6: 验 applyServerExpGrant 写入 (character.level === 5)
    // ---------------------------------------------------------------------
    const c2 = useCharacterStore.getState().character!;
    expect(c2.level).toBe(5);
    expect(c2.exp).toBe(0);
    expect(c2.expToNext).toBe(600);

    // ---------------------------------------------------------------------
    // Step 7: 卸 GuildClassModal, 挂 TierUnlockModal
    // ---------------------------------------------------------------------
    unmountGuild();
    render(<TierUnlockModal />);
    expect(screen.getByTestId('tier-unlock-modal')).toBeTruthy();
    expect(screen.getByTestId('tier-node-warrior_t2_1')).toBeTruthy();
    expect(screen.getByTestId('tier-node-warrior_t2_2')).toBeTruthy();
    expect(screen.getByTestId('tier-node-warrior_t2_3')).toBeTruthy();

    // ---------------------------------------------------------------------
    // Step 8: 点 T2 节点 → setClass append + PATCH /class (累计 2 次)
    // ---------------------------------------------------------------------
    fireEvent.click(screen.getByTestId('tier-node-warrior_t2_1'));
    const c3 = useCharacterStore.getState().character!;
    expect(c3.classSkills).toHaveLength(2);
    expect(c3.classSkills[0].nodeId).toBe('warrior_t1_1');
    expect(c3.classSkills[1].nodeId).toBe('warrior_t2_1');
    // tier PATCH 异步发出 (TierUnlockModal.handlePick 内部)
    await new Promise((r) => setTimeout(r, 30)); // 等 microtask
    expect(classPatchCalls).toBe(2);

    // ---------------------------------------------------------------------
    // Step 9: 清理
    // ---------------------------------------------------------------------
    unsub();
  });
});
```

并把外层 `describe('v0.5.7 — LevelUp 端到端链路', ...)` 删掉 (合并到内层 `describe` 里)。

- [ ] **Step 2: 跑测试, 期望 PASS**

Run: `cd f:\OpenTaleRunner\client && npx vitest run tests/integration/levelUpFlow.test.tsx`

Expected: PASS, "1 passed".

如果失败, 常见原因 + 修复:
- `Cannot find name 'RequestInit'` → 在文件顶部加 `/// <reference lib="dom" />` 或 `import type { RequestInit } from 'undici-types'`. 先看 vitest 是否能自动解析, 不行再加。
- `eventBus.emit` 不存在 → 检查 `import { eventBus } from '../../src/services/event/EventBus'` 路径。
- `classPatchCalls` 期望 1 但拿到 0 → 说明 GuildClassModal 还没来得及 async PATCH, 把 `expect(classPatchCalls).toBe(1)` 后移: 在 Step 4 之后 (`await new Promise((r) => setTimeout(r, 30))`)。
- `expPatchCalls` 期望 1 但拿到 0 → 说明 debounce 没 flush, 加 `await new Promise((r) => setTimeout(r, 500))` 排查。
- `screen.getByTestId('tier-unlock-modal')` 找不到 → 检查 `phase` 是否还是 'idle' (active/settled 都会让 modal 隐藏), 跑 `console.log(useCombatStore.getState())` 确认。

- [ ] **Step 3: 跑 10 次, 验证 0 flaky**

Run:
```bash
cd f:\OpenTaleRunner\client
for ($i=1; $i -le 10; $i++) {
  $out = npx vitest run tests/integration/levelUpFlow.test.tsx 2>&1 | Out-String
  if ($out.Contains("1 passed")) { Write-Output "Run $i`: PASS" }
  elseif ($out.Contains("failed")) { Write-Output "Run $i`: FAIL" }
  else { Write-Output "Run $i`: UNKNOWN" }
}
```

Expected: 10/10 PASS.

- [ ] **Step 4: 跑全 client 测试, 确认没破坏其他文件 (938 → 939)**

Run: `cd f:\OpenTaleRunner\client && npm run test:run`

Expected: "Test Files 84 passed (84)", "Tests 939 passed (939)" (1 new test).

- [ ] **Step 5: 跑 typecheck + lint, 确认没引入新 error**

Run:
```bash
cd f:\OpenTaleRunner\client && npm run typecheck
cd f:\OpenTaleRunner\client && npm run lint
```

Expected: 两个都 exit 0.

- [ ] **Step 6: Commit**

```bash
cd f:\OpenTaleRunner
git add client/tests/integration/levelUpFlow.test.tsx
git commit -m "test(integration): v0.5.7 — L1→L5+T2 完整链路 e2e

单一剧本式 it() 走完 7 个状态跃迁, 12 个 micro-step 标签:
  Step 0: character L1, classId=null
  Step 1: subscribeCharacterExpEvents(debounce 200ms)
  Step 2: GuildClassModal 选 warrior + T1 → PATCH /class 1次
  Step 3: emit 10xCOMBAT_HIT + 1xCOMBAT_KILL + 1xCOMBAT_END.victory
  Step 4: await debounce + fetch
  Step 5: PATCH /exp called once, amount=45, difficulty=normal
  Step 6: applyServerExpGrant 写入 → character.level=5
  Step 7: 挂 TierUnlockModal, 3 T2 选项可见
  Step 8: 点 T2 → setClass append → PATCH /class 2次
  Step 9: cleanup

补 v0.5.6 CHANGELOG parking lot 留的端到端回归网。
与 guild.test.tsx 区别: 那个用 grantExp() 推 level, 不验事件链;
本测试验 event→subscriber→PATCH 契约。

连跑 10/10 稳定. 全 client: 84/84 文件 939/939 tests.
typecheck + lint: 0 error."
```

---

## Task 4: 同步更新中文文档

**Files:**
- Modify: `docs/zh/角色系统.md`

- [ ] **Step 1: 找到 v0.5 章末尾**

Run: `cd f:\OpenTaleRunner && grep -n "v0\.5\.[0-9]\|## " docs/zh/角色系统.md | tail -20`

找到最后一个 v0.5.x 段 (应该是 v0.5.6 或 v0.5.5)。

- [ ] **Step 2: 在末尾追加 v0.5.7 段**

在该段下方追加:

```markdown
### v0.5.7 — 端到端回归测试

新增 `client/tests/integration/levelUpFlow.test.tsx`, 用单一剧本式
`it()` 走完 v0.5 主路径的 7 个状态跃迁, 作为 v0.5 的"集成回归网":

| 跃迁 | 验证点 |
|------|--------|
| 1. 战斗事件 emit | `eventBus.emit(COMBAT_HIT × 10 + COMBAT_KILL + COMBAT_END.victory)` |
| 2. subscriber 聚合 | 200ms debounce 窗口合并为单次 PATCH |
| 3. PATCH /exp | `fetch` 被叫一次, body `{amount:45, difficulty:'normal'}` |
| 4. 服务端算新 level | mock 越级返 L5 patch (`{level:5, exp:0, expToNext:600, ...}`) |
| 5. applyServerExpGrant | `character.level === 5` 写入 store |
| 6. TierUnlockModal 弹 | L5 + T1 已选 → `pendingTierChoice` 返回 2 → 3 T2 选项可见 |
| 7. 选 T2 节点 + PATCH /class | `setClass` append → `fetch /class` 累计 2 次 (含 Guild 时那 1 次) |

与 `tests/integration/guild.test.tsx` 既有"完整链路"测试的区别:
那个用 `grantExp()` 推 level, **跳过事件层**; 本测试验的是
**event → subscriber → PATCH** 契约, 是 v0.5.4 audit Gap #1 的回归网。

测试文件 150 行, 连跑 10/10 稳定. 全 client: 84/84 文件 939/939 tests.
`npm run typecheck` + `npm run lint` 0 error.
```

- [ ] **Step 3: Commit**

```bash
cd f:\OpenTaleRunner
git add docs/zh/角色系统.md
git commit -m "docs(zh): v0.5.7 — 端到端回归测试 (levelUpFlow)"
```

---

## Task 5: 同步更新英文文档

**Files:**
- Modify: `docs/en/Character-System.md`

- [ ] **Step 1: 找到 v0.5 章末尾**

Run: `cd f:\OpenTaleRunner && grep -n "v0\.5\.[0-9]\|## " docs/en/Character-System.md | tail -20`

- [ ] **Step 2: 在对应位置追加 v0.5.7 段**

把 Task 4 Step 2 的中文段落**整段翻译**为英文 (与 zh 内容对齐), 保持表格 + 段结构:

```markdown
### v0.5.7 — End-to-end regression test

Added `client/tests/integration/levelUpFlow.test.tsx`, a single-script
`it()` that walks through all 7 state transitions of the v0.5 main
path, serving as the v0.5 "integration regression net":

| Transition | Verification |
|------------|--------------|
| 1. Combat event emit | `eventBus.emit(COMBAT_HIT × 10 + COMBAT_KILL + COMBAT_END.victory)` |
| 2. subscriber aggregation | 200ms debounce window merges to single PATCH |
| 3. PATCH /exp | `fetch` called once, body `{amount:45, difficulty:'normal'}` |
| 4. server computes new level | mock jumps to L5 patch (`{level:5, exp:0, expToNext:600, ...}`) |
| 5. applyServerExpGrant | `character.level === 5` written to store |
| 6. TierUnlockModal pops | L5 + T1 already picked → `pendingTierChoice` returns 2 → 3 T2 options visible |
| 7. pick T2 node + PATCH /class | `setClass` appends → `fetch /class` accumulates 2 calls (1 from Guild earlier) |

Difference from the existing "full chain" test in
`tests/integration/guild.test.tsx`: that one uses `grantExp()` to push
the level, **skipping the event layer**; this one verifies the
**event → subscriber → PATCH** contract — the regression net for
v0.5.4 audit Gap #1.

Test file ~150 lines, 10/10 runs stable. Full client: 84/84 files,
939/939 tests. `npm run typecheck` + `npm run lint` 0 errors.
```

- [ ] **Step 3: Commit**

```bash
cd f:\OpenTaleRunner
git add docs/en/Character-System.md
git commit -m "docs(en): v0.5.7 — end-to-end regression test (levelUpFlow)"
```

---

## Task 6: CHANGELOG + 整体跑测 + tag

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 在 CHANGELOG 顶部加 v0.5.7 段**

在 `# Changelog\n\n## v0.5.6 — ...` 之前插入:

```markdown
## v0.5.7 — 2026-06-06 (LevelUp end-to-end integration test)

Closes the v0.5.6 followup "no e2e test for the combat→EXP→level→tier
chain" by adding a single-script `it()` that walks all 7 state
transitions of the v0.5 main path. No production code changes; no
public API changes; pure test + docs.

### What's covered (12 micro-steps in 1 it())

- Step 0: character L1, no class, no classId
- Step 1: `subscribeCharacterExpEvents({ debounceMs: 200 })`
- Step 2: `GuildClassModal` opens, pick `warrior + warrior_t1_1`,
  `setClass` writes locally, async PATCH `/class` is issued
- Step 3: emit `COMBAT_HIT ×10 + COMBAT_KILL ×1 + COMBAT_END.victory ×1`
  (totals 45 exp)
- Step 4: await 300ms (200ms debounce + slack)
- Step 5: assert PATCH `/exp` called exactly once with
  `body === {amount:45, difficulty:'normal'}`
- Step 6: mock server jumps the response to L5
  (`{level:5, exp:0, expToNext:600, unspentAttributePoints:0}`);
  `applyServerExpGrant` writes → `character.level === 5`
- Step 7: unmount `GuildClassModal`, render `TierUnlockModal`,
  assert 3 T2 options visible
- Step 8: click `warrior_t2_1`, `setClass` appends,
  async PATCH `/class` (now 2 calls total)
- Step 9: cleanup (unsub + eventBus.clear + fetch restore)

### Why this is not redundant with `tests/integration/guild.test.tsx`

The existing "full chain" test in `guild.test.tsx` uses `grantExp()` to
push the level up, **skipping the event layer**. This new test verifies
the **event → subscriber → PATCH** contract — exactly the layer that
v0.5.4 audit Gap #1 (broken EXP trigger chain) was about. So the two
tests are complementary, not duplicates.

### Test results

- **Client**: 10/10 stable runs of the new test; full client suite
  84/84 files, 939/939 tests pass.
- **typecheck + lint**: 0 errors (no new ones introduced).
- **Server**: not touched; 51/51 still pass.

### Docs

- `docs/zh/角色系统.md` — v0.5.7 段说明
- `docs/en/Character-System.md` — same translated

### Out of scope (parking lot, unchanged from v0.5.6)

- multi-PATCH failure / retry chain
- real server end-to-end (would need a server process in CI)
- multiplayer EXP sharing (v0.6)
- L1 → L20 (3 more tier picks)

---

## v0.5.6 — 2026-06-06 (v0.5 hotfix-debt cleanup)
```

- [ ] **Step 2: 整体跑测 + lint + typecheck 验证**

Run:
```bash
cd f:\OpenTaleRunner\client && npm run test:run
cd f:\OpenTaleRunner\client && npm run typecheck
cd f:\OpenTaleRunner\client && npm run lint
```

Expected:
- test:run → 84/84 文件, 939/939 tests
- typecheck → exit 0
- lint → exit 0

- [ ] **Step 3: Commit + tag**

```bash
cd f:\OpenTaleRunner
git add CHANGELOG.md
git commit -m "docs: v0.5.7 changelog"
git tag -a v0.5.7 -m "v0.5.7 — LevelUp end-to-end integration test

新增 client/tests/integration/levelUpFlow.test.tsx, 单一剧本式 it()
走完 v0.5 主路径 7 个状态跃迁 (战斗→EXP 事件→subscriber 聚合→
PATCH /exp→L5→TierUnlockModal→选 T2→PATCH /class).

补 v0.5.6 followup 'no e2e test' 留下的空缺. 与 guild.test.tsx 的
既有'完整链路'测试互补 (那个跳过事件层, 本测试验 event→subscriber
→PATCH 契约).

无 production 代码改动, 无 API 改动. 仅测试 + 文档.
- test:run 938 → 939
- typecheck + lint: 0 error
- 10/10 跑稳定"
git log --oneline -5
```

- [ ] **Step 4: 验证 tag 存在**

Run: `cd f:\OpenTaleRunner && git tag -l v0.5.7`

Expected: 输出 `v0.5.7`.

---

## Self-Review Notes

**Spec coverage check:**
- §0 背景 (空缺 + 后果) → Task 3 解决 (e2e test)
- §1 目标 (单一剧本式) → Task 3
- §2.1 文件位置 → Task 1
- §2.2 测试结构 (单一 it) → Task 3
- §2.3 12 micro-step 剧本 → Task 3
- §2.4 mock 策略 (vi.spyOn + debounce 200ms) → Task 3
- §2.5 不动 production 代码 → 整计划遵守
- §3 数据形状 (makeChar) → Task 2
- §4 错误处理 (cleanup) → Task 2 (afterEach) + Task 3 (unsub)
- §5 文档同步 (zh + en + CHANGELOG) → Task 4/5/6
- §7 验收 (10/10 稳定, 939 tests, typecheck+lint 0) → Task 3 Step 3-5 + Task 6 Step 2

**Placeholder scan:** 无 "TODO"/"TBD"/"implement later"。

**Type consistency:**
- `Character` 类型在 Task 2 定义, Task 3 引用 → 一致
- `EventName` / `EVENTS` 路径在 Step 1 import, Task 3 使用 → 一致
- `subscribeCharacterExpEvents({ debounceMs: 200 })` 在 Step 1/3 一致
- `data-testid` 字符串 (`class-option-warrior`, `t1-node-warrior_t1_1`, `tier-unlock-modal`, `tier-node-warrior_t2_1`) 与 `GuildClassModal.tsx` / `TierUnlockModal.tsx` 实际渲染的 id 一致

**Risk:**
- `RequestInit` 在 vitest 环境下可能需要 `/// <reference lib="dom" />` (Task 3 Step 2 fallback 提到)。
- vi.spyOn(fetch) 在 vitest 4.x 默认 mock 行为可能变化, 备选方案用 `vi.fn()` + `globalThis.fetch = ...`。
