# v0.6.6 属性架构统一 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 移除 `applyItemEffects` 对 attributes 的直接修改，改为纯计算模式，修复属性双重计算 bug

**Architecture:** `character.attributes` 只存纯基础值，装备/天赋加成通过 `computeAttributeBreakdowns` / `effectiveAttribute` 实时计算。加载旧角色时自动修正 attributes。

**Tech Stack:** TypeScript, Zustand, React, Vitest

---

### Task 1: 移除 `applyItemEffects` — characterStore.ts

**Files:**
- Modify: `client/src/stores/characterStore.ts`

- [ ] **Step 1: 删除 `applyItemEffects` action 的完整实现和接口声明**

在 `CharacterState` 接口中删除:
```typescript
/** 应用/反应用 物品的 attribute_mod 词条, true=装备, false=卸下 */
applyItemEffects: (item: Item, apply: boolean) => void;
```

在 store 实现中删除整个 `applyItemEffects` 方法（约 L190-L208）。

- [ ] **Step 2: 验证 TSC 编译**

Run: `cd client && npx tsc --noEmit`
Expected: 报错指向所有调用 `applyItemEffects` 的地方（BackpackModal, DebugModeModal, 测试文件）

---

### Task 2: 移除调用方 — BackpackModal.tsx

**Files:**
- Modify: `client/src/components/modals/BackpackModal.tsx`

- [ ] **Step 1: 删除 `applyItemEffects` 的 import 和调用**

1. 删除 `const applyItemEffects = useCharacterStore((s) => s.applyItemEffects);`
2. 在 `equipItem` 函数中删除:
   - `if (current) applyItemEffects(current, false);`
   - `applyItemEffects(item, true);`
3. 在 `unequipItem` 函数中删除:
   - `applyItemEffects(current, false);`

装备/卸下只修改 `inventory.equipped`，不再修改 attributes。

---

### Task 3: 移除调用方 — DebugModeModal.tsx

**Files:**
- Modify: `client/src/components/modals/DebugModeModal.tsx`

- [ ] **Step 1: 删除 `applyItemEffects` 调用**

在 `handleGenerateItem` 的自动装备逻辑中删除:
- `if (oldEquipped) { useCharacterStore.getState().applyItemEffects(oldEquipped, false); }`
- `useCharacterStore.getState().applyItemEffects(item, true);`

保留 `updateInventory` 调用（装备槽位更新）和 toast 提示。

---

### Task 4: 旧数据迁移 — setCharacter 自动修正

**Files:**
- Modify: `client/src/stores/characterStore.ts`

- [ ] **Step 1: 在 `setCharacter` 中加入属性修正逻辑**

当加载角色时，遍历已装备物品的 `attribute_mod` effects，从 `attributes` 中减去这些加成，还原纯基础值。

```typescript
setCharacter: (char) => {
  // v0.6.6: 迁移 — 从 attributes 中减去已装备物品的 attribute_mod，还原纯基础值
  const correctedAttrs = { ...char.attributes };
  const slots = [char.inventory.equipped.weapon, char.inventory.equipped.armor, char.inventory.equipped.accessory];
  for (const item of slots) {
    if (!item?.effects) continue;
    for (const eff of item.effects) {
      if (eff.type === 'attribute_mod' && typeof eff.value === 'object' && eff.value !== null) {
        const mods = eff.value as Record<string, unknown>;
        for (const key of Object.keys(correctedAttrs) as (keyof Attributes)[]) {
          const delta = mods[key];
          if (typeof delta === 'number') {
            correctedAttrs[key] = correctedAttrs[key] - delta;
          }
        }
      }
    }
  }
  set({ character: { ...char, attributes: correctedAttrs }, isLoaded: true });
},
```

---

### Task 5: computeAttributeBreakdowns 单元测试

**Files:**
- Modify: `client/tests/services/class/classEffects.test.ts`

- [ ] **Step 1: 添加 `computeAttributeBreakdowns` 测试**

测试场景：
1. 无装备无职业 → equipment=0, classTalent=0, total=base
2. 有装备 attribute_mod → equipment 正确累加
3. 有职业天赋 attribute_mod → classTalent 正确
4. 装备+天赋混合 → total = base + equipment + classTalent
5. 多件装备同属性 → equipment 累加

---

### Task 6: setCharacter 迁移测试

**Files:**
- Create: `client/tests/stores/characterStore-migration.test.ts`

- [ ] **Step 1: 编写迁移测试**

测试场景：
1. 加载含装备加成的旧角色 → attributes 自动减去装备加成
2. 加载无装备的角色 → attributes 不变
3. 加载含多件装备的角色 → 所有装备加成都被减去

---

### Task 7: 运行全量测试 + TSC + 提交

- [ ] **Step 1: 运行 TSC**
- [ ] **Step 2: 运行全量测试**
- [ ] **Step 3: 修复任何失败的测试**
- [ ] **Step 4: 提交并推送**
