# v0.5.9 — 死债文档同步 9 件 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 9 件"文档说未做但代码已做"的死债从文档里删掉,让文档与代码真实状态对齐。**纯文档清理,不动代码**。

**Architecture:**
- 死债 ID 对应 spec §0 表 (`9e49287`):
  - **#1** applyOverrides mode 限制 — JSON schema slot 限制 replace, narrative 自由 (代码已正确)
  - **#4** updateReputation 不处理 charisma — `applyConsequences.ts:170-180` 已转 CHA
  - **#5** sceneModifier 硬编码 0 — `PMEngine.ts:346/413` 已读 `scene_modifier`
  - **#6** mp/maxMp 暂未使用 — `combatStore.ts:266` `c.maxMp ?? 0` 已读
  - **#14** combatStore ↔ combatEngine 重复 — 实际不重复 (store 持状态, engine 是 reducer)
  - **#15** itemRegistry ↔ affixPools 重叠 — 不重叠 (runtime cache vs static data)
  - **#16** codexStore 6 分类硬编码 — 已简化成 1 类
  - **#17** effectTypeCompat 13 种映射 — 已完成 + ItemCallbackRouter 消费
  - **#21** "七层 Prompt 架构" vs 11 个 build — PromptBuilder 939 行 11 个 `build*`, 文档需对齐

**Tech Stack:** Markdown 文档,git grep 验证

**Spec:** `docs/superpowers/specs/2026-06-07-v0.5.9-v0.5.13-old-debt-sweep-design.md` §v0.5.9 (commit `9e49287` + 修订 `a2791dc`)

**继承自:** v0.5.8 (`6934fca` + tag `v0.5.8`)

---

## File Structure

**Modify (8 docs, 9 处死债):**
- `docs/en/Client-Architecture.md` — 删 #14 #15 (2 处)
- `docs/en/Character-System.md` — 删 #4 (1 处)
- `docs/en/Combat-System.md` — 删 #5 #6 #17 (3 处)
- `docs/en/PM-Engine-and-Prompt-System.md` — 删 #1 #21 (2 处)
- `docs/zh/客户端架构与机制.md` — 同步 #14 #15
- `docs/zh/角色系统.md` — 同步 #4
- `docs/zh/战斗系统.md` — 同步 #5 #6 #17
- `docs/zh/PM引擎与Prompt系统.md` — 同步 #1 #21

**Do NOT touch:**
- 任何 `client/src/**` — 文档与代码已对
- `CHANGELOG.md` — 已在 v0.5.6/7/8 记过

---

## Task 1: 死债现状基线 grep

**Files:**
- 读: 8 个 docs 文件

- [ ] **Step 1: 跑基线 grep 找到 9 处死债描述**

Run (PowerShell):
```powershell
cd "f:\OpenTaleRunner"
$patterns = @{
  '#4 charisma' = 'charisma'
  '#5 scene_modifier' = 'sceneModifier|scene_modifier'
  '#6 mp/maxMp' = 'mp/maxMp|maxMp.*未使用'
  '#14 store.*engine 重复' = 'combatStore.*combatEngine'
  '#15 itemRegistry.*affixPools' = 'itemRegistryStore.*affixPools'
  '#16 codexStore 6 类' = 'codexStore.*6 分类|six categor'
  '#17 effectTypeCompat' = 'effectTypeCompat'
  '#1 mode 限制' = 'applyOverrides.*mode'
  '#21 七层' = '七层|7 层|7-layer|seven-layer'
}
foreach ($k in $patterns.Keys) {
  Write-Host "=== $k ==="
  Select-String -Pattern $patterns[$k] docs/en docs/zh -List
}
```

Expected: 找到所有 9 处死债描述的位置 (en + zh 各一份)。

- [ ] **Step 2: 记录所有 line 号到本 plan 的执行备注**

为每个死债 ID 记录 (file:line) 方便后续 task 定位。

---

## Task 2: 删 en/Client-Architecture.md 死债 #14 #15

**Files:**
- Modify: `docs/en/Client-Architecture.md`

- [ ] **Step 1: 读现状定位 #14 #15**

Read: 找 "combatStore.*combatEngine" 和 "itemRegistryStore.*affixPools" 描述段

- [ ] **Step 2: 删 #14 段**

删除 "combatStore 与 combatEngine 状态重复" 整段 (含前后空行)。**注意保留前后段落结构**。

- [ ] **Step 3: 删 #15 段**

删除 "itemRegistryStore 与 affixPools 重叠" 整段。

- [ ] **Step 4: 跑 grep 验证**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "combatStore.*combatEngine|itemRegistryStore.*affixPools" docs/en/Client-Architecture.md`

Expected: 0 matches.

---

## Task 3: 删 en/Character-System.md 死债 #4

**Files:**
- Modify: `docs/en/Character-System.md`

- [ ] **Step 1: 读现状定位 #4**

Read: 找 "charisma" 上下文段 (在 "Pending v0.4 completion" 或类似段)

- [ ] **Step 2: 删 charisma 未处理描述**

把 "(TODO: charisma 未处理)" 或类似描述删掉,保留段其他内容。

- [ ] **Step 3: grep 验证**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "charisma" docs/en/Character-System.md`

Expected: 仅剩代码引用 (CharacterCard schema) ,无 "未处理" 描述。

---

## Task 4: 删 en/Combat-System.md 死债 #5 #6 #17

**Files:**
- Modify: `docs/en/Combat-System.md`

- [ ] **Step 1: 读现状定位 3 段**

Read: 找 "sceneModifier 硬编码 0" / "mp/maxMp 暂未使用" / "effectTypeCompat 待完成" 段

- [ ] **Step 2: 删 #5 段 (sceneModifier)**

- [ ] **Step 3: 删 #6 段 (mp/maxMp)**

- [ ] **Step 4: 删 #17 段 (effectTypeCompat)**

- [ ] **Step 5: grep 验证**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "sceneModifier.*0|mp/maxMp|effectTypeCompat.*TODO" docs/en/Combat-System.md`

Expected: 0 matches.

---

## Task 5: 删 en/PM-Engine-and-Prompt-System.md 死债 #1 #21

**Files:**
- Modify: `docs/en/PM-Engine-and-Prompt-System.md`

- [ ] **Step 1: 读现状定位 #1 段**

Read: 找 "applyOverrides mode 限制" 段 → 改为陈述事实 (JSON schema slot 限制 replace, narrative 自由)

- [ ] **Step 2: 改写 #1 段为陈述事实**

把 "TODO" / "未实现" 改写为 "当前行为: ..."

- [ ] **Step 3: 读现状定位 #21 段**

Read: 找 "七层 Prompt 架构" 段

- [ ] **Step 4: 改写 #21 段**

把 "七层" 描述改为 "11 个 build* 方法",列出方法名清单:

```
PromptBuilder 实际方法:
  private:
    - buildGhostEncounterHint
    - buildCharacterLayerSlim
    - buildSceneLayerSlim
    - buildCombinedAdvancePromptString
  public (7):
    - buildWorldLayer / buildCharacterLayer / buildSceneLayer
    - buildNarrativeContext / buildRulesLayer
    - buildAdvancePrompt / buildScenePrompt
```

- [ ] **Step 5: grep 验证**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "七层|7-layer" docs/en/PM-Engine-and-Prompt-System.md`

Expected: 0 matches.

---

## Task 6: 同步 4 个 zh 文档

**Files:**
- Modify: `docs/zh/客户端架构与机制.md`
- Modify: `docs/zh/角色系统.md`
- Modify: `docs/zh/战斗系统.md`
- Modify: `docs/zh/PM引擎与Prompt系统.md`

- [ ] **Step 1: 同步客户端架构与机制.md (#14 #15)**

镜像 Task 2 步骤,删中文版对应段。

- [ ] **Step 2: 同步角色系统.md (#4)**

镜像 Task 3 步骤。

- [ ] **Step 3: 同步战斗系统.md (#5 #6 #17)**

镜像 Task 4 步骤。

- [ ] **Step 4: 同步 PM 引擎与Prompt系统.md (#1 #21)**

镜像 Task 5 步骤。

- [ ] **Step 5: 跑全 zh grep 验证**

Run: `cd "f:\OpenTaleRunner" && Select-String -Pattern "七层|sceneModifier.*0|mp/maxMp|effectTypeCompat.*TODO|charisma" docs/zh`

Expected: 仅剩代码引用,无 "未实现/TODO/待" 描述。

---

## Task 7: 跑 client 全测验证 + Commit

**Files:**
- Modify: 8 docs 文件
- Test: 跑全 client test 仍 86 文件 939 tests pass

- [ ] **Step 1: 跑 client 全测**

Run: `cd "f:\OpenTaleRunner" && npm test -- --run 2>&1 | Select-String -Pattern "Test Files|Tests"`

Expected: 86 files / 939 tests pass (与 v0.5.8 基线一致)。

- [ ] **Step 2: 跑 lint**

Run: `cd "f:\OpenTaleRunner" && npm run lint 2>&1 | Select-Object -Last 20`

Expected: 0 errors (文档改动不影响 lint)。

- [ ] **Step 3: 写 commit message 文件**

Write `client/debug_commit_msg.txt`:
```
docs: v0.5.9 — sync 9 stale debt items to actual code state

- en/zh Client-Architecture.md: drop #14 combatStore/engine duplicate,
  #15 itemRegistry/affixPools overlap (both never existed)
- en/zh Character-System.md: drop #4 charisma "TODO" (already wired
  in applyConsequences.ts:170-180)
- en/zh Combat-System.md: drop #5 sceneModifier hardcoded 0 (already
  read in PMEngine.ts:346/413), #6 mp/maxMp unused (combatStore.ts:266
  reads maxMp ?? 0), #17 effectTypeCompat 13 mapping (done + consumed
  by ItemCallbackRouter)
- en/zh PM-Engine-and-Prompt-System.md: reword #1 applyOverrides mode
  limit as fact, align #21 "7-layer" with 11 build* methods
  (4 private + 7 public)

Pure documentation sync. No code changes.

Refs: v0.5.9 spec, commit 9e49287 + 修订 a2791dc
```

- [ ] **Step 4: Commit**

Run: `cd "f:\OpenTaleRunner" && git add docs/ && git commit -F client/debug_commit_msg.txt && rm client/debug_commit_msg.txt`

Expected: 1 commit,8 files changed.

- [ ] **Step 5: Tag v0.5.9**

Run: `cd "f:\OpenTaleRunner" && git tag -a v0.5.9 -m "v0.5.9 — stale debt doc sync (9 items)"`

Expected: tag `v0.5.9` created.

---

## 验收

- [ ] git grep 验证 9 件死债描述已删
- [ ] git diff 仅修改 docs/(8 文件)
- [ ] 全 client test 仍 86 文件 939 tests pass
- [ ] tag `v0.5.9` 已打
